"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MsgUpdateParamsResponse = exports.MsgUpdateParams = exports.MsgMirrorGroupResponse = exports.MsgMirrorGroup = exports.MsgMirrorBucketResponse = exports.MsgUpdateObjectInfo = exports.MsgUpdateObjectInfoResponse = exports.MsgMirrorBucket = exports.MsgMirrorObjectResponse = exports.MsgMirrorObject = exports.MsgDeletePolicyResponse = exports.MsgDeletePolicy = exports.MsgPutPolicyResponse = exports.MsgPutPolicy = exports.MsgCancelCreateObjectResponse = exports.MsgCancelCreateObject = exports.MsgUpdateBucketInfoResponse = exports.MsgUpdateBucketInfo = exports.MsgLeaveGroupResponse = exports.MsgLeaveGroup = exports.MsgUpdateGroupExtraResponse = exports.MsgUpdateGroupExtra = exports.MsgGroupMember = exports.MsgRenewGroupMemberResponse = exports.MsgRenewGroupMember = exports.MsgUpdateGroupMemberResponse = exports.MsgUpdateGroupMember = exports.MsgDeleteGroupResponse = exports.MsgDeleteGroup = exports.MsgCreateGroupResponse = exports.MsgCreateGroup = exports.MsgDiscontinueObjectResponse = exports.MsgDiscontinueObject = exports.MsgDeleteObjectResponse = exports.MsgDeleteObject = exports.MsgCopyObjectResponse = exports.MsgCopyObject = exports.MsgRejectSealObjectResponse = exports.MsgRejectSealObject = exports.MsgSealObjectResponse = exports.MsgSealObject = exports.MsgCreateObjectResponse = exports.MsgCreateObject = exports.MsgDiscontinueBucketResponse = exports.MsgDiscontinueBucket = exports.MsgDeleteBucketResponse = exports.MsgDeleteBucket = exports.MsgCreateBucketResponse = exports.MsgCreateBucket = exports.protobufPackage = void 0;
exports.MsgClientImpl = exports.MsgSetTagResponse = exports.MsgSetTag = exports.MsgRejectMigrateBucketResponse = exports.MsgRejectMigrateBucket = exports.MsgCancelMigrateBucketResponse = exports.MsgCancelMigrateBucket = exports.MsgCompleteMigrateBucketResponse = exports.MsgCompleteMigrateBucket = exports.MsgMigrateBucketResponse = exports.MsgMigrateBucket = void 0;
//@ts-nocheck
/* eslint-disable */
const common_1 = require("./common");
const approval_1 = require("../common/approval");
const wrapper_1 = require("../common/wrapper");
const common_2 = require("../permission/common");
const timestamp_1 = require("../../google/protobuf/timestamp");
const params_1 = require("./params");
const types_1 = require("./types");
const helpers_1 = require("../../helpers");
const _m0 = __importStar(require("protobufjs/minimal"));
exports.protobufPackage = "greenfield.storage";
function createBaseMsgCreateBucket() {
    return {
        creator: "",
        bucketName: "",
        visibility: 0,
        paymentAddress: "",
        primarySpAddress: "",
        primarySpApproval: undefined,
        chargedReadQuota: helpers_1.Long.UZERO
    };
}
exports.MsgCreateBucket = {
    typeUrl: "/greenfield.storage.MsgCreateBucket",
    encode(message, writer = _m0.Writer.create()) {
        if (message.creator !== "") {
            writer.uint32(10).string(message.creator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        if (message.visibility !== 0) {
            writer.uint32(24).int32(message.visibility);
        }
        if (message.paymentAddress !== "") {
            writer.uint32(34).string(message.paymentAddress);
        }
        if (message.primarySpAddress !== "") {
            writer.uint32(42).string(message.primarySpAddress);
        }
        if (message.primarySpApproval !== undefined) {
            approval_1.Approval.encode(message.primarySpApproval, writer.uint32(50).fork()).ldelim();
        }
        if (!message.chargedReadQuota.isZero()) {
            writer.uint32(56).uint64(message.chargedReadQuota);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCreateBucket();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.creator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.visibility = reader.int32();
                    break;
                case 4:
                    message.paymentAddress = reader.string();
                    break;
                case 5:
                    message.primarySpAddress = reader.string();
                    break;
                case 6:
                    message.primarySpApproval = approval_1.Approval.decode(reader, reader.uint32());
                    break;
                case 7:
                    message.chargedReadQuota = reader.uint64();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            creator: (0, helpers_1.isSet)(object.creator) ? String(object.creator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            visibility: (0, helpers_1.isSet)(object.visibility) ? (0, common_1.visibilityTypeFromJSON)(object.visibility) : -1,
            paymentAddress: (0, helpers_1.isSet)(object.paymentAddress) ? String(object.paymentAddress) : "",
            primarySpAddress: (0, helpers_1.isSet)(object.primarySpAddress) ? String(object.primarySpAddress) : "",
            primarySpApproval: (0, helpers_1.isSet)(object.primarySpApproval) ? approval_1.Approval.fromJSON(object.primarySpApproval) : undefined,
            chargedReadQuota: (0, helpers_1.isSet)(object.chargedReadQuota) ? helpers_1.Long.fromValue(object.chargedReadQuota) : helpers_1.Long.UZERO
        };
    },
    toJSON(message) {
        const obj = {};
        message.creator !== undefined && (obj.creator = message.creator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.visibility !== undefined && (obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility));
        message.paymentAddress !== undefined && (obj.paymentAddress = message.paymentAddress);
        message.primarySpAddress !== undefined && (obj.primarySpAddress = message.primarySpAddress);
        message.primarySpApproval !== undefined && (obj.primarySpApproval = message.primarySpApproval ? approval_1.Approval.toJSON(message.primarySpApproval) : undefined);
        message.chargedReadQuota !== undefined && (obj.chargedReadQuota = (message.chargedReadQuota || helpers_1.Long.UZERO).toString());
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgCreateBucket();
        message.creator = object.creator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.visibility = object.visibility ?? 0;
        message.paymentAddress = object.paymentAddress ?? "";
        message.primarySpAddress = object.primarySpAddress ?? "";
        message.primarySpApproval = object.primarySpApproval !== undefined && object.primarySpApproval !== null ? approval_1.Approval.fromPartial(object.primarySpApproval) : undefined;
        message.chargedReadQuota = object.chargedReadQuota !== undefined && object.chargedReadQuota !== null ? helpers_1.Long.fromValue(object.chargedReadQuota) : helpers_1.Long.UZERO;
        return message;
    },
    fromSDK(object) {
        return {
            creator: object?.creator,
            bucketName: object?.bucket_name,
            visibility: (0, helpers_1.isSet)(object.visibility) ? (0, common_1.visibilityTypeFromJSON)(object.visibility) : -1,
            paymentAddress: object?.payment_address,
            primarySpAddress: object?.primary_sp_address,
            primarySpApproval: object.primary_sp_approval ? approval_1.Approval.fromSDK(object.primary_sp_approval) : undefined,
            chargedReadQuota: object?.charged_read_quota
        };
    },
    toSDK(message) {
        const obj = {};
        obj.creator = message.creator;
        obj.bucket_name = message.bucketName;
        message.visibility !== undefined && (obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility));
        obj.payment_address = message.paymentAddress;
        obj.primary_sp_address = message.primarySpAddress;
        message.primarySpApproval !== undefined && (obj.primary_sp_approval = message.primarySpApproval ? approval_1.Approval.toSDK(message.primarySpApproval) : undefined);
        obj.charged_read_quota = message.chargedReadQuota;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgCreateBucket();
        if (object.creator !== undefined && object.creator !== null) {
            message.creator = object.creator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.visibility !== undefined && object.visibility !== null) {
            message.visibility = (0, common_1.visibilityTypeFromJSON)(object.visibility);
        }
        if (object.payment_address !== undefined && object.payment_address !== null) {
            message.paymentAddress = object.payment_address;
        }
        if (object.primary_sp_address !== undefined && object.primary_sp_address !== null) {
            message.primarySpAddress = object.primary_sp_address;
        }
        if (object.primary_sp_approval !== undefined && object.primary_sp_approval !== null) {
            message.primarySpApproval = approval_1.Approval.fromAmino(object.primary_sp_approval);
        }
        if (object.charged_read_quota !== undefined && object.charged_read_quota !== null) {
            message.chargedReadQuota = helpers_1.Long.fromString(object.charged_read_quota);
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.creator = message.creator;
        obj.bucket_name = message.bucketName;
        obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility);
        obj.payment_address = message.paymentAddress;
        obj.primary_sp_address = message.primarySpAddress;
        obj.primary_sp_approval = message.primarySpApproval ? approval_1.Approval.toAmino(message.primarySpApproval) : undefined;
        obj.charged_read_quota = message.chargedReadQuota ? message.chargedReadQuota.toString() : undefined;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCreateBucket.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCreateBucket.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCreateBucket.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCreateBucket",
            value: exports.MsgCreateBucket.encode(message).finish()
        };
    }
};
function createBaseMsgCreateBucketResponse() {
    return {
        bucketId: ""
    };
}
exports.MsgCreateBucketResponse = {
    typeUrl: "/greenfield.storage.MsgCreateBucketResponse",
    encode(message, writer = _m0.Writer.create()) {
        if (message.bucketId !== "") {
            writer.uint32(10).string(message.bucketId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCreateBucketResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.bucketId = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            bucketId: (0, helpers_1.isSet)(object.bucketId) ? String(object.bucketId) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.bucketId !== undefined && (obj.bucketId = message.bucketId);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgCreateBucketResponse();
        message.bucketId = object.bucketId ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            bucketId: object?.bucket_id
        };
    },
    toSDK(message) {
        const obj = {};
        obj.bucket_id = message.bucketId;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgCreateBucketResponse();
        if (object.bucket_id !== undefined && object.bucket_id !== null) {
            message.bucketId = object.bucket_id;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.bucket_id = message.bucketId;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCreateBucketResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCreateBucketResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCreateBucketResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCreateBucketResponse",
            value: exports.MsgCreateBucketResponse.encode(message).finish()
        };
    }
};
function createBaseMsgDeleteBucket() {
    return {
        operator: "",
        bucketName: ""
    };
}
exports.MsgDeleteBucket = {
    typeUrl: "/greenfield.storage.MsgDeleteBucket",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDeleteBucket();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgDeleteBucket();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgDeleteBucket();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDeleteBucket.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDeleteBucket.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDeleteBucket.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDeleteBucket",
            value: exports.MsgDeleteBucket.encode(message).finish()
        };
    }
};
function createBaseMsgDeleteBucketResponse() {
    return {};
}
exports.MsgDeleteBucketResponse = {
    typeUrl: "/greenfield.storage.MsgDeleteBucketResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDeleteBucketResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgDeleteBucketResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgDeleteBucketResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDeleteBucketResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDeleteBucketResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDeleteBucketResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDeleteBucketResponse",
            value: exports.MsgDeleteBucketResponse.encode(message).finish()
        };
    }
};
function createBaseMsgDiscontinueBucket() {
    return {
        operator: "",
        bucketName: "",
        reason: ""
    };
}
exports.MsgDiscontinueBucket = {
    typeUrl: "/greenfield.storage.MsgDiscontinueBucket",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        if (message.reason !== "") {
            writer.uint32(26).string(message.reason);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDiscontinueBucket();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.reason = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            reason: (0, helpers_1.isSet)(object.reason) ? String(object.reason) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.reason !== undefined && (obj.reason = message.reason);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgDiscontinueBucket();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.reason = object.reason ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name,
            reason: object?.reason
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.reason = message.reason;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgDiscontinueBucket();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.reason !== undefined && object.reason !== null) {
            message.reason = object.reason;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.reason = message.reason;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDiscontinueBucket.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDiscontinueBucket.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDiscontinueBucket.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDiscontinueBucket",
            value: exports.MsgDiscontinueBucket.encode(message).finish()
        };
    }
};
function createBaseMsgDiscontinueBucketResponse() {
    return {};
}
exports.MsgDiscontinueBucketResponse = {
    typeUrl: "/greenfield.storage.MsgDiscontinueBucketResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDiscontinueBucketResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgDiscontinueBucketResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgDiscontinueBucketResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDiscontinueBucketResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDiscontinueBucketResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDiscontinueBucketResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDiscontinueBucketResponse",
            value: exports.MsgDiscontinueBucketResponse.encode(message).finish()
        };
    }
};
function createBaseMsgCreateObject() {
    return {
        creator: "",
        bucketName: "",
        objectName: "",
        payloadSize: helpers_1.Long.UZERO,
        visibility: 0,
        contentType: "",
        primarySpApproval: undefined,
        expectChecksums: [],
        redundancyType: 0
    };
}
exports.MsgCreateObject = {
    typeUrl: "/greenfield.storage.MsgCreateObject",
    encode(message, writer = _m0.Writer.create()) {
        if (message.creator !== "") {
            writer.uint32(10).string(message.creator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        if (message.objectName !== "") {
            writer.uint32(26).string(message.objectName);
        }
        if (!message.payloadSize.isZero()) {
            writer.uint32(32).uint64(message.payloadSize);
        }
        if (message.visibility !== 0) {
            writer.uint32(40).int32(message.visibility);
        }
        if (message.contentType !== "") {
            writer.uint32(50).string(message.contentType);
        }
        if (message.primarySpApproval !== undefined) {
            approval_1.Approval.encode(message.primarySpApproval, writer.uint32(58).fork()).ldelim();
        }
        for (const v of message.expectChecksums) {
            writer.uint32(66).bytes(v);
        }
        if (message.redundancyType !== 0) {
            writer.uint32(72).int32(message.redundancyType);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCreateObject();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.creator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.objectName = reader.string();
                    break;
                case 4:
                    message.payloadSize = reader.uint64();
                    break;
                case 5:
                    message.visibility = reader.int32();
                    break;
                case 6:
                    message.contentType = reader.string();
                    break;
                case 7:
                    message.primarySpApproval = approval_1.Approval.decode(reader, reader.uint32());
                    break;
                case 8:
                    message.expectChecksums.push(reader.bytes());
                    break;
                case 9:
                    message.redundancyType = reader.int32();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            creator: (0, helpers_1.isSet)(object.creator) ? String(object.creator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            objectName: (0, helpers_1.isSet)(object.objectName) ? String(object.objectName) : "",
            payloadSize: (0, helpers_1.isSet)(object.payloadSize) ? helpers_1.Long.fromValue(object.payloadSize) : helpers_1.Long.UZERO,
            visibility: (0, helpers_1.isSet)(object.visibility) ? (0, common_1.visibilityTypeFromJSON)(object.visibility) : -1,
            contentType: (0, helpers_1.isSet)(object.contentType) ? String(object.contentType) : "",
            primarySpApproval: (0, helpers_1.isSet)(object.primarySpApproval) ? approval_1.Approval.fromJSON(object.primarySpApproval) : undefined,
            expectChecksums: Array.isArray(object?.expectChecksums) ? object.expectChecksums.map((e) => (0, helpers_1.bytesFromBase64)(e)) : [],
            redundancyType: (0, helpers_1.isSet)(object.redundancyType) ? (0, common_1.redundancyTypeFromJSON)(object.redundancyType) : -1
        };
    },
    toJSON(message) {
        const obj = {};
        message.creator !== undefined && (obj.creator = message.creator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.objectName !== undefined && (obj.objectName = message.objectName);
        message.payloadSize !== undefined && (obj.payloadSize = (message.payloadSize || helpers_1.Long.UZERO).toString());
        message.visibility !== undefined && (obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility));
        message.contentType !== undefined && (obj.contentType = message.contentType);
        message.primarySpApproval !== undefined && (obj.primarySpApproval = message.primarySpApproval ? approval_1.Approval.toJSON(message.primarySpApproval) : undefined);
        if (message.expectChecksums) {
            obj.expectChecksums = message.expectChecksums.map(e => (0, helpers_1.base64FromBytes)(e !== undefined ? e : new Uint8Array()));
        }
        else {
            obj.expectChecksums = [];
        }
        message.redundancyType !== undefined && (obj.redundancyType = (0, common_1.redundancyTypeToJSON)(message.redundancyType));
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgCreateObject();
        message.creator = object.creator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.objectName = object.objectName ?? "";
        message.payloadSize = object.payloadSize !== undefined && object.payloadSize !== null ? helpers_1.Long.fromValue(object.payloadSize) : helpers_1.Long.UZERO;
        message.visibility = object.visibility ?? 0;
        message.contentType = object.contentType ?? "";
        message.primarySpApproval = object.primarySpApproval !== undefined && object.primarySpApproval !== null ? approval_1.Approval.fromPartial(object.primarySpApproval) : undefined;
        message.expectChecksums = object.expectChecksums?.map(e => e) || [];
        message.redundancyType = object.redundancyType ?? 0;
        return message;
    },
    fromSDK(object) {
        return {
            creator: object?.creator,
            bucketName: object?.bucket_name,
            objectName: object?.object_name,
            payloadSize: object?.payload_size,
            visibility: (0, helpers_1.isSet)(object.visibility) ? (0, common_1.visibilityTypeFromJSON)(object.visibility) : -1,
            contentType: object?.content_type,
            primarySpApproval: object.primary_sp_approval ? approval_1.Approval.fromSDK(object.primary_sp_approval) : undefined,
            expectChecksums: Array.isArray(object?.expect_checksums) ? object.expect_checksums.map((e) => e) : [],
            redundancyType: (0, helpers_1.isSet)(object.redundancy_type) ? (0, common_1.redundancyTypeFromJSON)(object.redundancy_type) : -1
        };
    },
    toSDK(message) {
        const obj = {};
        obj.creator = message.creator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        obj.payload_size = message.payloadSize;
        message.visibility !== undefined && (obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility));
        obj.content_type = message.contentType;
        message.primarySpApproval !== undefined && (obj.primary_sp_approval = message.primarySpApproval ? approval_1.Approval.toSDK(message.primarySpApproval) : undefined);
        if (message.expectChecksums) {
            obj.expect_checksums = message.expectChecksums.map(e => e);
        }
        else {
            obj.expect_checksums = [];
        }
        message.redundancyType !== undefined && (obj.redundancy_type = (0, common_1.redundancyTypeToJSON)(message.redundancyType));
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgCreateObject();
        if (object.creator !== undefined && object.creator !== null) {
            message.creator = object.creator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.object_name !== undefined && object.object_name !== null) {
            message.objectName = object.object_name;
        }
        if (object.payload_size !== undefined && object.payload_size !== null) {
            message.payloadSize = helpers_1.Long.fromString(object.payload_size);
        }
        if (object.visibility !== undefined && object.visibility !== null) {
            message.visibility = (0, common_1.visibilityTypeFromJSON)(object.visibility);
        }
        if (object.content_type !== undefined && object.content_type !== null) {
            message.contentType = object.content_type;
        }
        if (object.primary_sp_approval !== undefined && object.primary_sp_approval !== null) {
            message.primarySpApproval = approval_1.Approval.fromAmino(object.primary_sp_approval);
        }
        message.expectChecksums = object.expect_checksums?.map(e => (0, helpers_1.bytesFromBase64)(e)) || [];
        if (object.redundancy_type !== undefined && object.redundancy_type !== null) {
            message.redundancyType = (0, common_1.redundancyTypeFromJSON)(object.redundancy_type);
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.creator = message.creator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        obj.payload_size = message.payloadSize ? message.payloadSize.toString() : undefined;
        obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility);
        obj.content_type = message.contentType;
        obj.primary_sp_approval = message.primarySpApproval ? approval_1.Approval.toAmino(message.primarySpApproval) : undefined;
        if (message.expectChecksums) {
            obj.expect_checksums = message.expectChecksums.map(e => (0, helpers_1.base64FromBytes)(e));
        }
        else {
            obj.expect_checksums = [];
        }
        obj.redundancy_type = (0, common_1.redundancyTypeToJSON)(message.redundancyType);
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCreateObject.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCreateObject.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCreateObject.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCreateObject",
            value: exports.MsgCreateObject.encode(message).finish()
        };
    }
};
function createBaseMsgCreateObjectResponse() {
    return {
        objectId: ""
    };
}
exports.MsgCreateObjectResponse = {
    typeUrl: "/greenfield.storage.MsgCreateObjectResponse",
    encode(message, writer = _m0.Writer.create()) {
        if (message.objectId !== "") {
            writer.uint32(10).string(message.objectId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCreateObjectResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.objectId = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            objectId: (0, helpers_1.isSet)(object.objectId) ? String(object.objectId) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.objectId !== undefined && (obj.objectId = message.objectId);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgCreateObjectResponse();
        message.objectId = object.objectId ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            objectId: object?.object_id
        };
    },
    toSDK(message) {
        const obj = {};
        obj.object_id = message.objectId;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgCreateObjectResponse();
        if (object.object_id !== undefined && object.object_id !== null) {
            message.objectId = object.object_id;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.object_id = message.objectId;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCreateObjectResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCreateObjectResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCreateObjectResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCreateObjectResponse",
            value: exports.MsgCreateObjectResponse.encode(message).finish()
        };
    }
};
function createBaseMsgSealObject() {
    return {
        operator: "",
        bucketName: "",
        objectName: "",
        globalVirtualGroupId: 0,
        secondarySpBlsAggSignatures: new Uint8Array()
    };
}
exports.MsgSealObject = {
    typeUrl: "/greenfield.storage.MsgSealObject",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        if (message.objectName !== "") {
            writer.uint32(26).string(message.objectName);
        }
        if (message.globalVirtualGroupId !== 0) {
            writer.uint32(32).uint32(message.globalVirtualGroupId);
        }
        if (message.secondarySpBlsAggSignatures.length !== 0) {
            writer.uint32(42).bytes(message.secondarySpBlsAggSignatures);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgSealObject();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.objectName = reader.string();
                    break;
                case 4:
                    message.globalVirtualGroupId = reader.uint32();
                    break;
                case 5:
                    message.secondarySpBlsAggSignatures = reader.bytes();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            objectName: (0, helpers_1.isSet)(object.objectName) ? String(object.objectName) : "",
            globalVirtualGroupId: (0, helpers_1.isSet)(object.globalVirtualGroupId) ? Number(object.globalVirtualGroupId) : 0,
            secondarySpBlsAggSignatures: (0, helpers_1.isSet)(object.secondarySpBlsAggSignatures) ? (0, helpers_1.bytesFromBase64)(object.secondarySpBlsAggSignatures) : new Uint8Array()
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.objectName !== undefined && (obj.objectName = message.objectName);
        message.globalVirtualGroupId !== undefined && (obj.globalVirtualGroupId = Math.round(message.globalVirtualGroupId));
        message.secondarySpBlsAggSignatures !== undefined && (obj.secondarySpBlsAggSignatures = (0, helpers_1.base64FromBytes)(message.secondarySpBlsAggSignatures !== undefined ? message.secondarySpBlsAggSignatures : new Uint8Array()));
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgSealObject();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.objectName = object.objectName ?? "";
        message.globalVirtualGroupId = object.globalVirtualGroupId ?? 0;
        message.secondarySpBlsAggSignatures = object.secondarySpBlsAggSignatures ?? new Uint8Array();
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name,
            objectName: object?.object_name,
            globalVirtualGroupId: object?.global_virtual_group_id,
            secondarySpBlsAggSignatures: object?.secondary_sp_bls_agg_signatures
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        obj.global_virtual_group_id = message.globalVirtualGroupId;
        obj.secondary_sp_bls_agg_signatures = message.secondarySpBlsAggSignatures;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgSealObject();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.object_name !== undefined && object.object_name !== null) {
            message.objectName = object.object_name;
        }
        if (object.global_virtual_group_id !== undefined && object.global_virtual_group_id !== null) {
            message.globalVirtualGroupId = object.global_virtual_group_id;
        }
        if (object.secondary_sp_bls_agg_signatures !== undefined && object.secondary_sp_bls_agg_signatures !== null) {
            message.secondarySpBlsAggSignatures = (0, helpers_1.bytesFromBase64)(object.secondary_sp_bls_agg_signatures);
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        obj.global_virtual_group_id = message.globalVirtualGroupId;
        obj.secondary_sp_bls_agg_signatures = message.secondarySpBlsAggSignatures ? (0, helpers_1.base64FromBytes)(message.secondarySpBlsAggSignatures) : undefined;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgSealObject.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgSealObject.decode(message.value);
    },
    toProto(message) {
        return exports.MsgSealObject.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgSealObject",
            value: exports.MsgSealObject.encode(message).finish()
        };
    }
};
function createBaseMsgSealObjectResponse() {
    return {};
}
exports.MsgSealObjectResponse = {
    typeUrl: "/greenfield.storage.MsgSealObjectResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgSealObjectResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgSealObjectResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgSealObjectResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgSealObjectResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgSealObjectResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgSealObjectResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgSealObjectResponse",
            value: exports.MsgSealObjectResponse.encode(message).finish()
        };
    }
};
function createBaseMsgRejectSealObject() {
    return {
        operator: "",
        bucketName: "",
        objectName: ""
    };
}
exports.MsgRejectSealObject = {
    typeUrl: "/greenfield.storage.MsgRejectSealObject",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        if (message.objectName !== "") {
            writer.uint32(26).string(message.objectName);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgRejectSealObject();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.objectName = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            objectName: (0, helpers_1.isSet)(object.objectName) ? String(object.objectName) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.objectName !== undefined && (obj.objectName = message.objectName);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgRejectSealObject();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.objectName = object.objectName ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name,
            objectName: object?.object_name
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgRejectSealObject();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.object_name !== undefined && object.object_name !== null) {
            message.objectName = object.object_name;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgRejectSealObject.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgRejectSealObject.decode(message.value);
    },
    toProto(message) {
        return exports.MsgRejectSealObject.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgRejectSealObject",
            value: exports.MsgRejectSealObject.encode(message).finish()
        };
    }
};
function createBaseMsgRejectSealObjectResponse() {
    return {};
}
exports.MsgRejectSealObjectResponse = {
    typeUrl: "/greenfield.storage.MsgRejectSealObjectResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgRejectSealObjectResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgRejectSealObjectResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgRejectSealObjectResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgRejectSealObjectResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgRejectSealObjectResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgRejectSealObjectResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgRejectSealObjectResponse",
            value: exports.MsgRejectSealObjectResponse.encode(message).finish()
        };
    }
};
function createBaseMsgCopyObject() {
    return {
        operator: "",
        srcBucketName: "",
        dstBucketName: "",
        srcObjectName: "",
        dstObjectName: "",
        dstPrimarySpApproval: undefined
    };
}
exports.MsgCopyObject = {
    typeUrl: "/greenfield.storage.MsgCopyObject",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.srcBucketName !== "") {
            writer.uint32(18).string(message.srcBucketName);
        }
        if (message.dstBucketName !== "") {
            writer.uint32(26).string(message.dstBucketName);
        }
        if (message.srcObjectName !== "") {
            writer.uint32(34).string(message.srcObjectName);
        }
        if (message.dstObjectName !== "") {
            writer.uint32(42).string(message.dstObjectName);
        }
        if (message.dstPrimarySpApproval !== undefined) {
            approval_1.Approval.encode(message.dstPrimarySpApproval, writer.uint32(50).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCopyObject();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.srcBucketName = reader.string();
                    break;
                case 3:
                    message.dstBucketName = reader.string();
                    break;
                case 4:
                    message.srcObjectName = reader.string();
                    break;
                case 5:
                    message.dstObjectName = reader.string();
                    break;
                case 6:
                    message.dstPrimarySpApproval = approval_1.Approval.decode(reader, reader.uint32());
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            srcBucketName: (0, helpers_1.isSet)(object.srcBucketName) ? String(object.srcBucketName) : "",
            dstBucketName: (0, helpers_1.isSet)(object.dstBucketName) ? String(object.dstBucketName) : "",
            srcObjectName: (0, helpers_1.isSet)(object.srcObjectName) ? String(object.srcObjectName) : "",
            dstObjectName: (0, helpers_1.isSet)(object.dstObjectName) ? String(object.dstObjectName) : "",
            dstPrimarySpApproval: (0, helpers_1.isSet)(object.dstPrimarySpApproval) ? approval_1.Approval.fromJSON(object.dstPrimarySpApproval) : undefined
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.srcBucketName !== undefined && (obj.srcBucketName = message.srcBucketName);
        message.dstBucketName !== undefined && (obj.dstBucketName = message.dstBucketName);
        message.srcObjectName !== undefined && (obj.srcObjectName = message.srcObjectName);
        message.dstObjectName !== undefined && (obj.dstObjectName = message.dstObjectName);
        message.dstPrimarySpApproval !== undefined && (obj.dstPrimarySpApproval = message.dstPrimarySpApproval ? approval_1.Approval.toJSON(message.dstPrimarySpApproval) : undefined);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgCopyObject();
        message.operator = object.operator ?? "";
        message.srcBucketName = object.srcBucketName ?? "";
        message.dstBucketName = object.dstBucketName ?? "";
        message.srcObjectName = object.srcObjectName ?? "";
        message.dstObjectName = object.dstObjectName ?? "";
        message.dstPrimarySpApproval = object.dstPrimarySpApproval !== undefined && object.dstPrimarySpApproval !== null ? approval_1.Approval.fromPartial(object.dstPrimarySpApproval) : undefined;
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            srcBucketName: object?.src_bucket_name,
            dstBucketName: object?.dst_bucket_name,
            srcObjectName: object?.src_object_name,
            dstObjectName: object?.dst_object_name,
            dstPrimarySpApproval: object.dst_primary_sp_approval ? approval_1.Approval.fromSDK(object.dst_primary_sp_approval) : undefined
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.src_bucket_name = message.srcBucketName;
        obj.dst_bucket_name = message.dstBucketName;
        obj.src_object_name = message.srcObjectName;
        obj.dst_object_name = message.dstObjectName;
        message.dstPrimarySpApproval !== undefined && (obj.dst_primary_sp_approval = message.dstPrimarySpApproval ? approval_1.Approval.toSDK(message.dstPrimarySpApproval) : undefined);
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgCopyObject();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.src_bucket_name !== undefined && object.src_bucket_name !== null) {
            message.srcBucketName = object.src_bucket_name;
        }
        if (object.dst_bucket_name !== undefined && object.dst_bucket_name !== null) {
            message.dstBucketName = object.dst_bucket_name;
        }
        if (object.src_object_name !== undefined && object.src_object_name !== null) {
            message.srcObjectName = object.src_object_name;
        }
        if (object.dst_object_name !== undefined && object.dst_object_name !== null) {
            message.dstObjectName = object.dst_object_name;
        }
        if (object.dst_primary_sp_approval !== undefined && object.dst_primary_sp_approval !== null) {
            message.dstPrimarySpApproval = approval_1.Approval.fromAmino(object.dst_primary_sp_approval);
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.src_bucket_name = message.srcBucketName;
        obj.dst_bucket_name = message.dstBucketName;
        obj.src_object_name = message.srcObjectName;
        obj.dst_object_name = message.dstObjectName;
        obj.dst_primary_sp_approval = message.dstPrimarySpApproval ? approval_1.Approval.toAmino(message.dstPrimarySpApproval) : undefined;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCopyObject.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCopyObject.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCopyObject.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCopyObject",
            value: exports.MsgCopyObject.encode(message).finish()
        };
    }
};
function createBaseMsgCopyObjectResponse() {
    return {
        objectId: ""
    };
}
exports.MsgCopyObjectResponse = {
    typeUrl: "/greenfield.storage.MsgCopyObjectResponse",
    encode(message, writer = _m0.Writer.create()) {
        if (message.objectId !== "") {
            writer.uint32(10).string(message.objectId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCopyObjectResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.objectId = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            objectId: (0, helpers_1.isSet)(object.objectId) ? String(object.objectId) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.objectId !== undefined && (obj.objectId = message.objectId);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgCopyObjectResponse();
        message.objectId = object.objectId ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            objectId: object?.object_id
        };
    },
    toSDK(message) {
        const obj = {};
        obj.object_id = message.objectId;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgCopyObjectResponse();
        if (object.object_id !== undefined && object.object_id !== null) {
            message.objectId = object.object_id;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.object_id = message.objectId;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCopyObjectResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCopyObjectResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCopyObjectResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCopyObjectResponse",
            value: exports.MsgCopyObjectResponse.encode(message).finish()
        };
    }
};
function createBaseMsgDeleteObject() {
    return {
        operator: "",
        bucketName: "",
        objectName: ""
    };
}
exports.MsgDeleteObject = {
    typeUrl: "/greenfield.storage.MsgDeleteObject",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        if (message.objectName !== "") {
            writer.uint32(26).string(message.objectName);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDeleteObject();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.objectName = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            objectName: (0, helpers_1.isSet)(object.objectName) ? String(object.objectName) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.objectName !== undefined && (obj.objectName = message.objectName);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgDeleteObject();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.objectName = object.objectName ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name,
            objectName: object?.object_name
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgDeleteObject();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.object_name !== undefined && object.object_name !== null) {
            message.objectName = object.object_name;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDeleteObject.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDeleteObject.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDeleteObject.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDeleteObject",
            value: exports.MsgDeleteObject.encode(message).finish()
        };
    }
};
function createBaseMsgDeleteObjectResponse() {
    return {};
}
exports.MsgDeleteObjectResponse = {
    typeUrl: "/greenfield.storage.MsgDeleteObjectResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDeleteObjectResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgDeleteObjectResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgDeleteObjectResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDeleteObjectResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDeleteObjectResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDeleteObjectResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDeleteObjectResponse",
            value: exports.MsgDeleteObjectResponse.encode(message).finish()
        };
    }
};
function createBaseMsgDiscontinueObject() {
    return {
        operator: "",
        bucketName: "",
        objectIds: [],
        reason: ""
    };
}
exports.MsgDiscontinueObject = {
    typeUrl: "/greenfield.storage.MsgDiscontinueObject",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        for (const v of message.objectIds) {
            writer.uint32(26).string(v);
        }
        if (message.reason !== "") {
            writer.uint32(34).string(message.reason);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDiscontinueObject();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.objectIds.push(reader.string());
                    break;
                case 4:
                    message.reason = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            objectIds: Array.isArray(object?.objectIds) ? object.objectIds.map((e) => String(e)) : [],
            reason: (0, helpers_1.isSet)(object.reason) ? String(object.reason) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        if (message.objectIds) {
            obj.objectIds = message.objectIds.map(e => e);
        }
        else {
            obj.objectIds = [];
        }
        message.reason !== undefined && (obj.reason = message.reason);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgDiscontinueObject();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.objectIds = object.objectIds?.map(e => e) || [];
        message.reason = object.reason ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name,
            objectIds: Array.isArray(object?.object_ids) ? object.object_ids.map((e) => e) : [],
            reason: object?.reason
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        if (message.objectIds) {
            obj.object_ids = message.objectIds.map(e => e);
        }
        else {
            obj.object_ids = [];
        }
        obj.reason = message.reason;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgDiscontinueObject();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        message.objectIds = object.object_ids?.map(e => e) || [];
        if (object.reason !== undefined && object.reason !== null) {
            message.reason = object.reason;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        if (message.objectIds) {
            obj.object_ids = message.objectIds.map(e => e);
        }
        else {
            obj.object_ids = [];
        }
        obj.reason = message.reason;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDiscontinueObject.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDiscontinueObject.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDiscontinueObject.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDiscontinueObject",
            value: exports.MsgDiscontinueObject.encode(message).finish()
        };
    }
};
function createBaseMsgDiscontinueObjectResponse() {
    return {};
}
exports.MsgDiscontinueObjectResponse = {
    typeUrl: "/greenfield.storage.MsgDiscontinueObjectResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDiscontinueObjectResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgDiscontinueObjectResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgDiscontinueObjectResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDiscontinueObjectResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDiscontinueObjectResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDiscontinueObjectResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDiscontinueObjectResponse",
            value: exports.MsgDiscontinueObjectResponse.encode(message).finish()
        };
    }
};
function createBaseMsgCreateGroup() {
    return {
        creator: "",
        groupName: "",
        extra: ""
    };
}
exports.MsgCreateGroup = {
    typeUrl: "/greenfield.storage.MsgCreateGroup",
    encode(message, writer = _m0.Writer.create()) {
        if (message.creator !== "") {
            writer.uint32(10).string(message.creator);
        }
        if (message.groupName !== "") {
            writer.uint32(18).string(message.groupName);
        }
        if (message.extra !== "") {
            writer.uint32(26).string(message.extra);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCreateGroup();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.creator = reader.string();
                    break;
                case 2:
                    message.groupName = reader.string();
                    break;
                case 3:
                    message.extra = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            creator: (0, helpers_1.isSet)(object.creator) ? String(object.creator) : "",
            groupName: (0, helpers_1.isSet)(object.groupName) ? String(object.groupName) : "",
            extra: (0, helpers_1.isSet)(object.extra) ? String(object.extra) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.creator !== undefined && (obj.creator = message.creator);
        message.groupName !== undefined && (obj.groupName = message.groupName);
        message.extra !== undefined && (obj.extra = message.extra);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgCreateGroup();
        message.creator = object.creator ?? "";
        message.groupName = object.groupName ?? "";
        message.extra = object.extra ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            creator: object?.creator,
            groupName: object?.group_name,
            extra: object?.extra
        };
    },
    toSDK(message) {
        const obj = {};
        obj.creator = message.creator;
        obj.group_name = message.groupName;
        obj.extra = message.extra;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgCreateGroup();
        if (object.creator !== undefined && object.creator !== null) {
            message.creator = object.creator;
        }
        if (object.group_name !== undefined && object.group_name !== null) {
            message.groupName = object.group_name;
        }
        if (object.extra !== undefined && object.extra !== null) {
            message.extra = object.extra;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.creator = message.creator;
        obj.group_name = message.groupName;
        obj.extra = message.extra;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCreateGroup.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCreateGroup.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCreateGroup.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCreateGroup",
            value: exports.MsgCreateGroup.encode(message).finish()
        };
    }
};
function createBaseMsgCreateGroupResponse() {
    return {
        groupId: ""
    };
}
exports.MsgCreateGroupResponse = {
    typeUrl: "/greenfield.storage.MsgCreateGroupResponse",
    encode(message, writer = _m0.Writer.create()) {
        if (message.groupId !== "") {
            writer.uint32(10).string(message.groupId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCreateGroupResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.groupId = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            groupId: (0, helpers_1.isSet)(object.groupId) ? String(object.groupId) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.groupId !== undefined && (obj.groupId = message.groupId);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgCreateGroupResponse();
        message.groupId = object.groupId ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            groupId: object?.group_id
        };
    },
    toSDK(message) {
        const obj = {};
        obj.group_id = message.groupId;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgCreateGroupResponse();
        if (object.group_id !== undefined && object.group_id !== null) {
            message.groupId = object.group_id;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.group_id = message.groupId;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCreateGroupResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCreateGroupResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCreateGroupResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCreateGroupResponse",
            value: exports.MsgCreateGroupResponse.encode(message).finish()
        };
    }
};
function createBaseMsgDeleteGroup() {
    return {
        operator: "",
        groupName: ""
    };
}
exports.MsgDeleteGroup = {
    typeUrl: "/greenfield.storage.MsgDeleteGroup",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.groupName !== "") {
            writer.uint32(18).string(message.groupName);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDeleteGroup();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.groupName = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            groupName: (0, helpers_1.isSet)(object.groupName) ? String(object.groupName) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.groupName !== undefined && (obj.groupName = message.groupName);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgDeleteGroup();
        message.operator = object.operator ?? "";
        message.groupName = object.groupName ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            groupName: object?.group_name
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.group_name = message.groupName;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgDeleteGroup();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.group_name !== undefined && object.group_name !== null) {
            message.groupName = object.group_name;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.group_name = message.groupName;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDeleteGroup.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDeleteGroup.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDeleteGroup.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDeleteGroup",
            value: exports.MsgDeleteGroup.encode(message).finish()
        };
    }
};
function createBaseMsgDeleteGroupResponse() {
    return {};
}
exports.MsgDeleteGroupResponse = {
    typeUrl: "/greenfield.storage.MsgDeleteGroupResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDeleteGroupResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgDeleteGroupResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgDeleteGroupResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDeleteGroupResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDeleteGroupResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDeleteGroupResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDeleteGroupResponse",
            value: exports.MsgDeleteGroupResponse.encode(message).finish()
        };
    }
};
function createBaseMsgUpdateGroupMember() {
    return {
        operator: "",
        groupOwner: "",
        groupName: "",
        membersToAdd: [],
        membersToDelete: []
    };
}
exports.MsgUpdateGroupMember = {
    typeUrl: "/greenfield.storage.MsgUpdateGroupMember",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.groupOwner !== "") {
            writer.uint32(18).string(message.groupOwner);
        }
        if (message.groupName !== "") {
            writer.uint32(26).string(message.groupName);
        }
        for (const v of message.membersToAdd) {
            exports.MsgGroupMember.encode(v, writer.uint32(34).fork()).ldelim();
        }
        for (const v of message.membersToDelete) {
            writer.uint32(42).string(v);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgUpdateGroupMember();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.groupOwner = reader.string();
                    break;
                case 3:
                    message.groupName = reader.string();
                    break;
                case 4:
                    message.membersToAdd.push(exports.MsgGroupMember.decode(reader, reader.uint32()));
                    break;
                case 5:
                    message.membersToDelete.push(reader.string());
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            groupOwner: (0, helpers_1.isSet)(object.groupOwner) ? String(object.groupOwner) : "",
            groupName: (0, helpers_1.isSet)(object.groupName) ? String(object.groupName) : "",
            membersToAdd: Array.isArray(object?.membersToAdd) ? object.membersToAdd.map((e) => exports.MsgGroupMember.fromJSON(e)) : [],
            membersToDelete: Array.isArray(object?.membersToDelete) ? object.membersToDelete.map((e) => String(e)) : []
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.groupOwner !== undefined && (obj.groupOwner = message.groupOwner);
        message.groupName !== undefined && (obj.groupName = message.groupName);
        if (message.membersToAdd) {
            obj.membersToAdd = message.membersToAdd.map(e => e ? exports.MsgGroupMember.toJSON(e) : undefined);
        }
        else {
            obj.membersToAdd = [];
        }
        if (message.membersToDelete) {
            obj.membersToDelete = message.membersToDelete.map(e => e);
        }
        else {
            obj.membersToDelete = [];
        }
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgUpdateGroupMember();
        message.operator = object.operator ?? "";
        message.groupOwner = object.groupOwner ?? "";
        message.groupName = object.groupName ?? "";
        message.membersToAdd = object.membersToAdd?.map(e => exports.MsgGroupMember.fromPartial(e)) || [];
        message.membersToDelete = object.membersToDelete?.map(e => e) || [];
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            groupOwner: object?.group_owner,
            groupName: object?.group_name,
            membersToAdd: Array.isArray(object?.members_to_add) ? object.members_to_add.map((e) => exports.MsgGroupMember.fromSDK(e)) : [],
            membersToDelete: Array.isArray(object?.members_to_delete) ? object.members_to_delete.map((e) => e) : []
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.group_owner = message.groupOwner;
        obj.group_name = message.groupName;
        if (message.membersToAdd) {
            obj.members_to_add = message.membersToAdd.map(e => e ? exports.MsgGroupMember.toSDK(e) : undefined);
        }
        else {
            obj.members_to_add = [];
        }
        if (message.membersToDelete) {
            obj.members_to_delete = message.membersToDelete.map(e => e);
        }
        else {
            obj.members_to_delete = [];
        }
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgUpdateGroupMember();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.group_owner !== undefined && object.group_owner !== null) {
            message.groupOwner = object.group_owner;
        }
        if (object.group_name !== undefined && object.group_name !== null) {
            message.groupName = object.group_name;
        }
        message.membersToAdd = object.members_to_add?.map(e => exports.MsgGroupMember.fromAmino(e)) || [];
        message.membersToDelete = object.members_to_delete?.map(e => e) || [];
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.group_owner = message.groupOwner;
        obj.group_name = message.groupName;
        if (message.membersToAdd) {
            obj.members_to_add = message.membersToAdd.map(e => e ? exports.MsgGroupMember.toAmino(e) : undefined);
        }
        else {
            obj.members_to_add = [];
        }
        if (message.membersToDelete) {
            obj.members_to_delete = message.membersToDelete.map(e => e);
        }
        else {
            obj.members_to_delete = [];
        }
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgUpdateGroupMember.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgUpdateGroupMember.decode(message.value);
    },
    toProto(message) {
        return exports.MsgUpdateGroupMember.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgUpdateGroupMember",
            value: exports.MsgUpdateGroupMember.encode(message).finish()
        };
    }
};
function createBaseMsgUpdateGroupMemberResponse() {
    return {};
}
exports.MsgUpdateGroupMemberResponse = {
    typeUrl: "/greenfield.storage.MsgUpdateGroupMemberResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgUpdateGroupMemberResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgUpdateGroupMemberResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgUpdateGroupMemberResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgUpdateGroupMemberResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgUpdateGroupMemberResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgUpdateGroupMemberResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgUpdateGroupMemberResponse",
            value: exports.MsgUpdateGroupMemberResponse.encode(message).finish()
        };
    }
};
function createBaseMsgRenewGroupMember() {
    return {
        operator: "",
        groupOwner: "",
        groupName: "",
        members: []
    };
}
exports.MsgRenewGroupMember = {
    typeUrl: "/greenfield.storage.MsgRenewGroupMember",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.groupOwner !== "") {
            writer.uint32(18).string(message.groupOwner);
        }
        if (message.groupName !== "") {
            writer.uint32(26).string(message.groupName);
        }
        for (const v of message.members) {
            exports.MsgGroupMember.encode(v, writer.uint32(34).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgRenewGroupMember();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.groupOwner = reader.string();
                    break;
                case 3:
                    message.groupName = reader.string();
                    break;
                case 4:
                    message.members.push(exports.MsgGroupMember.decode(reader, reader.uint32()));
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            groupOwner: (0, helpers_1.isSet)(object.groupOwner) ? String(object.groupOwner) : "",
            groupName: (0, helpers_1.isSet)(object.groupName) ? String(object.groupName) : "",
            members: Array.isArray(object?.members) ? object.members.map((e) => exports.MsgGroupMember.fromJSON(e)) : []
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.groupOwner !== undefined && (obj.groupOwner = message.groupOwner);
        message.groupName !== undefined && (obj.groupName = message.groupName);
        if (message.members) {
            obj.members = message.members.map(e => e ? exports.MsgGroupMember.toJSON(e) : undefined);
        }
        else {
            obj.members = [];
        }
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgRenewGroupMember();
        message.operator = object.operator ?? "";
        message.groupOwner = object.groupOwner ?? "";
        message.groupName = object.groupName ?? "";
        message.members = object.members?.map(e => exports.MsgGroupMember.fromPartial(e)) || [];
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            groupOwner: object?.group_owner,
            groupName: object?.group_name,
            members: Array.isArray(object?.members) ? object.members.map((e) => exports.MsgGroupMember.fromSDK(e)) : []
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.group_owner = message.groupOwner;
        obj.group_name = message.groupName;
        if (message.members) {
            obj.members = message.members.map(e => e ? exports.MsgGroupMember.toSDK(e) : undefined);
        }
        else {
            obj.members = [];
        }
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgRenewGroupMember();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.group_owner !== undefined && object.group_owner !== null) {
            message.groupOwner = object.group_owner;
        }
        if (object.group_name !== undefined && object.group_name !== null) {
            message.groupName = object.group_name;
        }
        message.members = object.members?.map(e => exports.MsgGroupMember.fromAmino(e)) || [];
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.group_owner = message.groupOwner;
        obj.group_name = message.groupName;
        if (message.members) {
            obj.members = message.members.map(e => e ? exports.MsgGroupMember.toAmino(e) : undefined);
        }
        else {
            obj.members = [];
        }
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgRenewGroupMember.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgRenewGroupMember.decode(message.value);
    },
    toProto(message) {
        return exports.MsgRenewGroupMember.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgRenewGroupMember",
            value: exports.MsgRenewGroupMember.encode(message).finish()
        };
    }
};
function createBaseMsgRenewGroupMemberResponse() {
    return {};
}
exports.MsgRenewGroupMemberResponse = {
    typeUrl: "/greenfield.storage.MsgRenewGroupMemberResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgRenewGroupMemberResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgRenewGroupMemberResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgRenewGroupMemberResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgRenewGroupMemberResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgRenewGroupMemberResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgRenewGroupMemberResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgRenewGroupMemberResponse",
            value: exports.MsgRenewGroupMemberResponse.encode(message).finish()
        };
    }
};
function createBaseMsgGroupMember() {
    return {
        member: "",
        expirationTime: undefined
    };
}
exports.MsgGroupMember = {
    typeUrl: "/greenfield.storage.MsgGroupMember",
    encode(message, writer = _m0.Writer.create()) {
        if (message.member !== "") {
            writer.uint32(10).string(message.member);
        }
        if (message.expirationTime !== undefined) {
            timestamp_1.Timestamp.encode(message.expirationTime, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgGroupMember();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.member = reader.string();
                    break;
                case 2:
                    message.expirationTime = timestamp_1.Timestamp.decode(reader, reader.uint32());
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            member: (0, helpers_1.isSet)(object.member) ? String(object.member) : "",
            expirationTime: (0, helpers_1.isSet)(object.expirationTime) ? (0, helpers_1.fromJsonTimestamp)(object.expirationTime) : undefined
        };
    },
    toJSON(message) {
        const obj = {};
        message.member !== undefined && (obj.member = message.member);
        message.expirationTime !== undefined && (obj.expirationTime = (0, helpers_1.fromTimestamp)(message.expirationTime).toISOString());
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgGroupMember();
        message.member = object.member ?? "";
        message.expirationTime = object.expirationTime !== undefined && object.expirationTime !== null ? timestamp_1.Timestamp.fromPartial(object.expirationTime) : undefined;
        return message;
    },
    fromSDK(object) {
        return {
            member: object?.member,
            expirationTime: object.expiration_time ? timestamp_1.Timestamp.fromSDK(object.expiration_time) : undefined
        };
    },
    toSDK(message) {
        const obj = {};
        obj.member = message.member;
        message.expirationTime !== undefined && (obj.expiration_time = message.expirationTime ? timestamp_1.Timestamp.toSDK(message.expirationTime) : undefined);
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgGroupMember();
        if (object.member !== undefined && object.member !== null) {
            message.member = object.member;
        }
        if (object.expiration_time !== undefined && object.expiration_time !== null) {
            message.expirationTime = timestamp_1.Timestamp.fromAmino(object.expiration_time);
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.member = message.member;
        obj.expiration_time = message.expirationTime ? timestamp_1.Timestamp.toAmino(message.expirationTime) : undefined;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgGroupMember.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgGroupMember.decode(message.value);
    },
    toProto(message) {
        return exports.MsgGroupMember.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgGroupMember",
            value: exports.MsgGroupMember.encode(message).finish()
        };
    }
};
function createBaseMsgUpdateGroupExtra() {
    return {
        operator: "",
        groupOwner: "",
        groupName: "",
        extra: ""
    };
}
exports.MsgUpdateGroupExtra = {
    typeUrl: "/greenfield.storage.MsgUpdateGroupExtra",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.groupOwner !== "") {
            writer.uint32(18).string(message.groupOwner);
        }
        if (message.groupName !== "") {
            writer.uint32(26).string(message.groupName);
        }
        if (message.extra !== "") {
            writer.uint32(34).string(message.extra);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgUpdateGroupExtra();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.groupOwner = reader.string();
                    break;
                case 3:
                    message.groupName = reader.string();
                    break;
                case 4:
                    message.extra = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            groupOwner: (0, helpers_1.isSet)(object.groupOwner) ? String(object.groupOwner) : "",
            groupName: (0, helpers_1.isSet)(object.groupName) ? String(object.groupName) : "",
            extra: (0, helpers_1.isSet)(object.extra) ? String(object.extra) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.groupOwner !== undefined && (obj.groupOwner = message.groupOwner);
        message.groupName !== undefined && (obj.groupName = message.groupName);
        message.extra !== undefined && (obj.extra = message.extra);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgUpdateGroupExtra();
        message.operator = object.operator ?? "";
        message.groupOwner = object.groupOwner ?? "";
        message.groupName = object.groupName ?? "";
        message.extra = object.extra ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            groupOwner: object?.group_owner,
            groupName: object?.group_name,
            extra: object?.extra
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.group_owner = message.groupOwner;
        obj.group_name = message.groupName;
        obj.extra = message.extra;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgUpdateGroupExtra();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.group_owner !== undefined && object.group_owner !== null) {
            message.groupOwner = object.group_owner;
        }
        if (object.group_name !== undefined && object.group_name !== null) {
            message.groupName = object.group_name;
        }
        if (object.extra !== undefined && object.extra !== null) {
            message.extra = object.extra;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.group_owner = message.groupOwner;
        obj.group_name = message.groupName;
        obj.extra = message.extra;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgUpdateGroupExtra.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgUpdateGroupExtra.decode(message.value);
    },
    toProto(message) {
        return exports.MsgUpdateGroupExtra.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgUpdateGroupExtra",
            value: exports.MsgUpdateGroupExtra.encode(message).finish()
        };
    }
};
function createBaseMsgUpdateGroupExtraResponse() {
    return {};
}
exports.MsgUpdateGroupExtraResponse = {
    typeUrl: "/greenfield.storage.MsgUpdateGroupExtraResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgUpdateGroupExtraResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgUpdateGroupExtraResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgUpdateGroupExtraResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgUpdateGroupExtraResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgUpdateGroupExtraResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgUpdateGroupExtraResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgUpdateGroupExtraResponse",
            value: exports.MsgUpdateGroupExtraResponse.encode(message).finish()
        };
    }
};
function createBaseMsgLeaveGroup() {
    return {
        member: "",
        groupOwner: "",
        groupName: ""
    };
}
exports.MsgLeaveGroup = {
    typeUrl: "/greenfield.storage.MsgLeaveGroup",
    encode(message, writer = _m0.Writer.create()) {
        if (message.member !== "") {
            writer.uint32(10).string(message.member);
        }
        if (message.groupOwner !== "") {
            writer.uint32(18).string(message.groupOwner);
        }
        if (message.groupName !== "") {
            writer.uint32(26).string(message.groupName);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgLeaveGroup();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.member = reader.string();
                    break;
                case 2:
                    message.groupOwner = reader.string();
                    break;
                case 3:
                    message.groupName = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            member: (0, helpers_1.isSet)(object.member) ? String(object.member) : "",
            groupOwner: (0, helpers_1.isSet)(object.groupOwner) ? String(object.groupOwner) : "",
            groupName: (0, helpers_1.isSet)(object.groupName) ? String(object.groupName) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.member !== undefined && (obj.member = message.member);
        message.groupOwner !== undefined && (obj.groupOwner = message.groupOwner);
        message.groupName !== undefined && (obj.groupName = message.groupName);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgLeaveGroup();
        message.member = object.member ?? "";
        message.groupOwner = object.groupOwner ?? "";
        message.groupName = object.groupName ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            member: object?.member,
            groupOwner: object?.group_owner,
            groupName: object?.group_name
        };
    },
    toSDK(message) {
        const obj = {};
        obj.member = message.member;
        obj.group_owner = message.groupOwner;
        obj.group_name = message.groupName;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgLeaveGroup();
        if (object.member !== undefined && object.member !== null) {
            message.member = object.member;
        }
        if (object.group_owner !== undefined && object.group_owner !== null) {
            message.groupOwner = object.group_owner;
        }
        if (object.group_name !== undefined && object.group_name !== null) {
            message.groupName = object.group_name;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.member = message.member;
        obj.group_owner = message.groupOwner;
        obj.group_name = message.groupName;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgLeaveGroup.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgLeaveGroup.decode(message.value);
    },
    toProto(message) {
        return exports.MsgLeaveGroup.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgLeaveGroup",
            value: exports.MsgLeaveGroup.encode(message).finish()
        };
    }
};
function createBaseMsgLeaveGroupResponse() {
    return {};
}
exports.MsgLeaveGroupResponse = {
    typeUrl: "/greenfield.storage.MsgLeaveGroupResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgLeaveGroupResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgLeaveGroupResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgLeaveGroupResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgLeaveGroupResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgLeaveGroupResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgLeaveGroupResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgLeaveGroupResponse",
            value: exports.MsgLeaveGroupResponse.encode(message).finish()
        };
    }
};
function createBaseMsgUpdateBucketInfo() {
    return {
        operator: "",
        bucketName: "",
        chargedReadQuota: undefined,
        paymentAddress: "",
        visibility: 0
    };
}
exports.MsgUpdateBucketInfo = {
    typeUrl: "/greenfield.storage.MsgUpdateBucketInfo",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        if (message.chargedReadQuota !== undefined) {
            wrapper_1.UInt64Value.encode(message.chargedReadQuota, writer.uint32(26).fork()).ldelim();
        }
        if (message.paymentAddress !== "") {
            writer.uint32(34).string(message.paymentAddress);
        }
        if (message.visibility !== 0) {
            writer.uint32(40).int32(message.visibility);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgUpdateBucketInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.chargedReadQuota = wrapper_1.UInt64Value.decode(reader, reader.uint32());
                    break;
                case 4:
                    message.paymentAddress = reader.string();
                    break;
                case 5:
                    message.visibility = reader.int32();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            chargedReadQuota: (0, helpers_1.isSet)(object.chargedReadQuota) ? wrapper_1.UInt64Value.fromJSON(object.chargedReadQuota) : undefined,
            paymentAddress: (0, helpers_1.isSet)(object.paymentAddress) ? String(object.paymentAddress) : "",
            visibility: (0, helpers_1.isSet)(object.visibility) ? (0, common_1.visibilityTypeFromJSON)(object.visibility) : -1
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.chargedReadQuota !== undefined && (obj.chargedReadQuota = message.chargedReadQuota ? wrapper_1.UInt64Value.toJSON(message.chargedReadQuota) : undefined);
        message.paymentAddress !== undefined && (obj.paymentAddress = message.paymentAddress);
        message.visibility !== undefined && (obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility));
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgUpdateBucketInfo();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.chargedReadQuota = object.chargedReadQuota !== undefined && object.chargedReadQuota !== null ? wrapper_1.UInt64Value.fromPartial(object.chargedReadQuota) : undefined;
        message.paymentAddress = object.paymentAddress ?? "";
        message.visibility = object.visibility ?? 0;
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name,
            chargedReadQuota: object.charged_read_quota ? wrapper_1.UInt64Value.fromSDK(object.charged_read_quota) : undefined,
            paymentAddress: object?.payment_address,
            visibility: (0, helpers_1.isSet)(object.visibility) ? (0, common_1.visibilityTypeFromJSON)(object.visibility) : -1
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        message.chargedReadQuota !== undefined && (obj.charged_read_quota = message.chargedReadQuota ? wrapper_1.UInt64Value.toSDK(message.chargedReadQuota) : undefined);
        obj.payment_address = message.paymentAddress;
        message.visibility !== undefined && (obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility));
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgUpdateBucketInfo();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.charged_read_quota !== undefined && object.charged_read_quota !== null) {
            message.chargedReadQuota = wrapper_1.UInt64Value.fromAmino(object.charged_read_quota);
        }
        if (object.payment_address !== undefined && object.payment_address !== null) {
            message.paymentAddress = object.payment_address;
        }
        if (object.visibility !== undefined && object.visibility !== null) {
            message.visibility = (0, common_1.visibilityTypeFromJSON)(object.visibility);
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.charged_read_quota = message.chargedReadQuota ? wrapper_1.UInt64Value.toAmino(message.chargedReadQuota) : undefined;
        obj.payment_address = message.paymentAddress;
        obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility);
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgUpdateBucketInfo.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgUpdateBucketInfo.decode(message.value);
    },
    toProto(message) {
        return exports.MsgUpdateBucketInfo.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgUpdateBucketInfo",
            value: exports.MsgUpdateBucketInfo.encode(message).finish()
        };
    }
};
function createBaseMsgUpdateBucketInfoResponse() {
    return {};
}
exports.MsgUpdateBucketInfoResponse = {
    typeUrl: "/greenfield.storage.MsgUpdateBucketInfoResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgUpdateBucketInfoResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgUpdateBucketInfoResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgUpdateBucketInfoResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgUpdateBucketInfoResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgUpdateBucketInfoResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgUpdateBucketInfoResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgUpdateBucketInfoResponse",
            value: exports.MsgUpdateBucketInfoResponse.encode(message).finish()
        };
    }
};
function createBaseMsgCancelCreateObject() {
    return {
        operator: "",
        bucketName: "",
        objectName: ""
    };
}
exports.MsgCancelCreateObject = {
    typeUrl: "/greenfield.storage.MsgCancelCreateObject",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        if (message.objectName !== "") {
            writer.uint32(26).string(message.objectName);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCancelCreateObject();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.objectName = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            objectName: (0, helpers_1.isSet)(object.objectName) ? String(object.objectName) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.objectName !== undefined && (obj.objectName = message.objectName);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgCancelCreateObject();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.objectName = object.objectName ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name,
            objectName: object?.object_name
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgCancelCreateObject();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.object_name !== undefined && object.object_name !== null) {
            message.objectName = object.object_name;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCancelCreateObject.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCancelCreateObject.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCancelCreateObject.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCancelCreateObject",
            value: exports.MsgCancelCreateObject.encode(message).finish()
        };
    }
};
function createBaseMsgCancelCreateObjectResponse() {
    return {};
}
exports.MsgCancelCreateObjectResponse = {
    typeUrl: "/greenfield.storage.MsgCancelCreateObjectResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCancelCreateObjectResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgCancelCreateObjectResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgCancelCreateObjectResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCancelCreateObjectResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCancelCreateObjectResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCancelCreateObjectResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCancelCreateObjectResponse",
            value: exports.MsgCancelCreateObjectResponse.encode(message).finish()
        };
    }
};
function createBaseMsgPutPolicy() {
    return {
        operator: "",
        principal: undefined,
        resource: "",
        statements: [],
        expirationTime: undefined
    };
}
exports.MsgPutPolicy = {
    typeUrl: "/greenfield.storage.MsgPutPolicy",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.principal !== undefined) {
            common_2.Principal.encode(message.principal, writer.uint32(18).fork()).ldelim();
        }
        if (message.resource !== "") {
            writer.uint32(26).string(message.resource);
        }
        for (const v of message.statements) {
            common_2.Statement.encode(v, writer.uint32(34).fork()).ldelim();
        }
        if (message.expirationTime !== undefined) {
            timestamp_1.Timestamp.encode(message.expirationTime, writer.uint32(58).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgPutPolicy();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.principal = common_2.Principal.decode(reader, reader.uint32());
                    break;
                case 3:
                    message.resource = reader.string();
                    break;
                case 4:
                    message.statements.push(common_2.Statement.decode(reader, reader.uint32()));
                    break;
                case 7:
                    message.expirationTime = timestamp_1.Timestamp.decode(reader, reader.uint32());
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            principal: (0, helpers_1.isSet)(object.principal) ? common_2.Principal.fromJSON(object.principal) : undefined,
            resource: (0, helpers_1.isSet)(object.resource) ? String(object.resource) : "",
            statements: Array.isArray(object?.statements) ? object.statements.map((e) => common_2.Statement.fromJSON(e)) : [],
            expirationTime: (0, helpers_1.isSet)(object.expirationTime) ? (0, helpers_1.fromJsonTimestamp)(object.expirationTime) : undefined
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.principal !== undefined && (obj.principal = message.principal ? common_2.Principal.toJSON(message.principal) : undefined);
        message.resource !== undefined && (obj.resource = message.resource);
        if (message.statements) {
            obj.statements = message.statements.map(e => e ? common_2.Statement.toJSON(e) : undefined);
        }
        else {
            obj.statements = [];
        }
        message.expirationTime !== undefined && (obj.expirationTime = (0, helpers_1.fromTimestamp)(message.expirationTime).toISOString());
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgPutPolicy();
        message.operator = object.operator ?? "";
        message.principal = object.principal !== undefined && object.principal !== null ? common_2.Principal.fromPartial(object.principal) : undefined;
        message.resource = object.resource ?? "";
        message.statements = object.statements?.map(e => common_2.Statement.fromPartial(e)) || [];
        message.expirationTime = object.expirationTime !== undefined && object.expirationTime !== null ? timestamp_1.Timestamp.fromPartial(object.expirationTime) : undefined;
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            principal: object.principal ? common_2.Principal.fromSDK(object.principal) : undefined,
            resource: object?.resource,
            statements: Array.isArray(object?.statements) ? object.statements.map((e) => common_2.Statement.fromSDK(e)) : [],
            expirationTime: object.expiration_time ? timestamp_1.Timestamp.fromSDK(object.expiration_time) : undefined
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        message.principal !== undefined && (obj.principal = message.principal ? common_2.Principal.toSDK(message.principal) : undefined);
        obj.resource = message.resource;
        if (message.statements) {
            obj.statements = message.statements.map(e => e ? common_2.Statement.toSDK(e) : undefined);
        }
        else {
            obj.statements = [];
        }
        message.expirationTime !== undefined && (obj.expiration_time = message.expirationTime ? timestamp_1.Timestamp.toSDK(message.expirationTime) : undefined);
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgPutPolicy();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.principal !== undefined && object.principal !== null) {
            message.principal = common_2.Principal.fromAmino(object.principal);
        }
        if (object.resource !== undefined && object.resource !== null) {
            message.resource = object.resource;
        }
        message.statements = object.statements?.map(e => common_2.Statement.fromAmino(e)) || [];
        if (object.expiration_time !== undefined && object.expiration_time !== null) {
            message.expirationTime = timestamp_1.Timestamp.fromAmino(object.expiration_time);
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.principal = message.principal ? common_2.Principal.toAmino(message.principal) : undefined;
        obj.resource = message.resource;
        if (message.statements) {
            obj.statements = message.statements.map(e => e ? common_2.Statement.toAmino(e) : undefined);
        }
        else {
            obj.statements = [];
        }
        obj.expiration_time = message.expirationTime ? timestamp_1.Timestamp.toAmino(message.expirationTime) : undefined;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgPutPolicy.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgPutPolicy.decode(message.value);
    },
    toProto(message) {
        return exports.MsgPutPolicy.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgPutPolicy",
            value: exports.MsgPutPolicy.encode(message).finish()
        };
    }
};
function createBaseMsgPutPolicyResponse() {
    return {
        policyId: ""
    };
}
exports.MsgPutPolicyResponse = {
    typeUrl: "/greenfield.storage.MsgPutPolicyResponse",
    encode(message, writer = _m0.Writer.create()) {
        if (message.policyId !== "") {
            writer.uint32(34).string(message.policyId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgPutPolicyResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 4:
                    message.policyId = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            policyId: (0, helpers_1.isSet)(object.policyId) ? String(object.policyId) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.policyId !== undefined && (obj.policyId = message.policyId);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgPutPolicyResponse();
        message.policyId = object.policyId ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            policyId: object?.policy_id
        };
    },
    toSDK(message) {
        const obj = {};
        obj.policy_id = message.policyId;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgPutPolicyResponse();
        if (object.policy_id !== undefined && object.policy_id !== null) {
            message.policyId = object.policy_id;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.policy_id = message.policyId;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgPutPolicyResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgPutPolicyResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgPutPolicyResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgPutPolicyResponse",
            value: exports.MsgPutPolicyResponse.encode(message).finish()
        };
    }
};
function createBaseMsgDeletePolicy() {
    return {
        operator: "",
        principal: undefined,
        resource: ""
    };
}
exports.MsgDeletePolicy = {
    typeUrl: "/greenfield.storage.MsgDeletePolicy",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.principal !== undefined) {
            common_2.Principal.encode(message.principal, writer.uint32(18).fork()).ldelim();
        }
        if (message.resource !== "") {
            writer.uint32(26).string(message.resource);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDeletePolicy();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.principal = common_2.Principal.decode(reader, reader.uint32());
                    break;
                case 3:
                    message.resource = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            principal: (0, helpers_1.isSet)(object.principal) ? common_2.Principal.fromJSON(object.principal) : undefined,
            resource: (0, helpers_1.isSet)(object.resource) ? String(object.resource) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.principal !== undefined && (obj.principal = message.principal ? common_2.Principal.toJSON(message.principal) : undefined);
        message.resource !== undefined && (obj.resource = message.resource);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgDeletePolicy();
        message.operator = object.operator ?? "";
        message.principal = object.principal !== undefined && object.principal !== null ? common_2.Principal.fromPartial(object.principal) : undefined;
        message.resource = object.resource ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            principal: object.principal ? common_2.Principal.fromSDK(object.principal) : undefined,
            resource: object?.resource
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        message.principal !== undefined && (obj.principal = message.principal ? common_2.Principal.toSDK(message.principal) : undefined);
        obj.resource = message.resource;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgDeletePolicy();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.principal !== undefined && object.principal !== null) {
            message.principal = common_2.Principal.fromAmino(object.principal);
        }
        if (object.resource !== undefined && object.resource !== null) {
            message.resource = object.resource;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.principal = message.principal ? common_2.Principal.toAmino(message.principal) : undefined;
        obj.resource = message.resource;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDeletePolicy.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDeletePolicy.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDeletePolicy.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDeletePolicy",
            value: exports.MsgDeletePolicy.encode(message).finish()
        };
    }
};
function createBaseMsgDeletePolicyResponse() {
    return {
        policyId: ""
    };
}
exports.MsgDeletePolicyResponse = {
    typeUrl: "/greenfield.storage.MsgDeletePolicyResponse",
    encode(message, writer = _m0.Writer.create()) {
        if (message.policyId !== "") {
            writer.uint32(34).string(message.policyId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgDeletePolicyResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 4:
                    message.policyId = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            policyId: (0, helpers_1.isSet)(object.policyId) ? String(object.policyId) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.policyId !== undefined && (obj.policyId = message.policyId);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgDeletePolicyResponse();
        message.policyId = object.policyId ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            policyId: object?.policy_id
        };
    },
    toSDK(message) {
        const obj = {};
        obj.policy_id = message.policyId;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgDeletePolicyResponse();
        if (object.policy_id !== undefined && object.policy_id !== null) {
            message.policyId = object.policy_id;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.policy_id = message.policyId;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgDeletePolicyResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgDeletePolicyResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgDeletePolicyResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgDeletePolicyResponse",
            value: exports.MsgDeletePolicyResponse.encode(message).finish()
        };
    }
};
function createBaseMsgMirrorObject() {
    return {
        operator: "",
        id: "",
        bucketName: "",
        objectName: "",
        destChainId: 0
    };
}
exports.MsgMirrorObject = {
    typeUrl: "/greenfield.storage.MsgMirrorObject",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.id !== "") {
            writer.uint32(18).string(message.id);
        }
        if (message.bucketName !== "") {
            writer.uint32(26).string(message.bucketName);
        }
        if (message.objectName !== "") {
            writer.uint32(34).string(message.objectName);
        }
        if (message.destChainId !== 0) {
            writer.uint32(40).uint32(message.destChainId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgMirrorObject();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.id = reader.string();
                    break;
                case 3:
                    message.bucketName = reader.string();
                    break;
                case 4:
                    message.objectName = reader.string();
                    break;
                case 5:
                    message.destChainId = reader.uint32();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            id: (0, helpers_1.isSet)(object.id) ? String(object.id) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            objectName: (0, helpers_1.isSet)(object.objectName) ? String(object.objectName) : "",
            destChainId: (0, helpers_1.isSet)(object.destChainId) ? Number(object.destChainId) : 0
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.id !== undefined && (obj.id = message.id);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.objectName !== undefined && (obj.objectName = message.objectName);
        message.destChainId !== undefined && (obj.destChainId = Math.round(message.destChainId));
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgMirrorObject();
        message.operator = object.operator ?? "";
        message.id = object.id ?? "";
        message.bucketName = object.bucketName ?? "";
        message.objectName = object.objectName ?? "";
        message.destChainId = object.destChainId ?? 0;
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            id: object?.id,
            bucketName: object?.bucket_name,
            objectName: object?.object_name,
            destChainId: object?.dest_chain_id
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.id = message.id;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        obj.dest_chain_id = message.destChainId;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgMirrorObject();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.id !== undefined && object.id !== null) {
            message.id = object.id;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.object_name !== undefined && object.object_name !== null) {
            message.objectName = object.object_name;
        }
        if (object.dest_chain_id !== undefined && object.dest_chain_id !== null) {
            message.destChainId = object.dest_chain_id;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.id = message.id;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        obj.dest_chain_id = message.destChainId;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgMirrorObject.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgMirrorObject.decode(message.value);
    },
    toProto(message) {
        return exports.MsgMirrorObject.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgMirrorObject",
            value: exports.MsgMirrorObject.encode(message).finish()
        };
    }
};
function createBaseMsgMirrorObjectResponse() {
    return {};
}
exports.MsgMirrorObjectResponse = {
    typeUrl: "/greenfield.storage.MsgMirrorObjectResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgMirrorObjectResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgMirrorObjectResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgMirrorObjectResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgMirrorObjectResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgMirrorObjectResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgMirrorObjectResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgMirrorObjectResponse",
            value: exports.MsgMirrorObjectResponse.encode(message).finish()
        };
    }
};
function createBaseMsgMirrorBucket() {
    return {
        operator: "",
        id: "",
        bucketName: "",
        destChainId: 0
    };
}
exports.MsgMirrorBucket = {
    typeUrl: "/greenfield.storage.MsgMirrorBucket",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.id !== "") {
            writer.uint32(18).string(message.id);
        }
        if (message.bucketName !== "") {
            writer.uint32(26).string(message.bucketName);
        }
        if (message.destChainId !== 0) {
            writer.uint32(32).uint32(message.destChainId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgMirrorBucket();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.id = reader.string();
                    break;
                case 3:
                    message.bucketName = reader.string();
                    break;
                case 4:
                    message.destChainId = reader.uint32();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            id: (0, helpers_1.isSet)(object.id) ? String(object.id) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            destChainId: (0, helpers_1.isSet)(object.destChainId) ? Number(object.destChainId) : 0
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.id !== undefined && (obj.id = message.id);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.destChainId !== undefined && (obj.destChainId = Math.round(message.destChainId));
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgMirrorBucket();
        message.operator = object.operator ?? "";
        message.id = object.id ?? "";
        message.bucketName = object.bucketName ?? "";
        message.destChainId = object.destChainId ?? 0;
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            id: object?.id,
            bucketName: object?.bucket_name,
            destChainId: object?.dest_chain_id
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.id = message.id;
        obj.bucket_name = message.bucketName;
        obj.dest_chain_id = message.destChainId;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgMirrorBucket();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.id !== undefined && object.id !== null) {
            message.id = object.id;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.dest_chain_id !== undefined && object.dest_chain_id !== null) {
            message.destChainId = object.dest_chain_id;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.id = message.id;
        obj.bucket_name = message.bucketName;
        obj.dest_chain_id = message.destChainId;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgMirrorBucket.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgMirrorBucket.decode(message.value);
    },
    toProto(message) {
        return exports.MsgMirrorBucket.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgMirrorBucket",
            value: exports.MsgMirrorBucket.encode(message).finish()
        };
    }
};
function createBaseMsgUpdateObjectInfoResponse() {
    return {};
}
exports.MsgUpdateObjectInfoResponse = {
    typeUrl: "/greenfield.storage.MsgUpdateObjectInfoResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgUpdateObjectInfoResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgUpdateObjectInfoResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgUpdateObjectInfoResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgUpdateObjectInfoResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgUpdateObjectInfoResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgUpdateObjectInfoResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgUpdateObjectInfoResponse",
            value: exports.MsgUpdateObjectInfoResponse.encode(message).finish()
        };
    }
};
function createBaseMsgUpdateObjectInfo() {
    return {
        operator: "",
        bucketName: "",
        objectName: "",
        visibility: 0
    };
}
exports.MsgUpdateObjectInfo = {
    typeUrl: "/greenfield.storage.MsgUpdateObjectInfo",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        if (message.objectName !== "") {
            writer.uint32(26).string(message.objectName);
        }
        if (message.visibility !== 0) {
            writer.uint32(32).int32(message.visibility);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgUpdateObjectInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.objectName = reader.string();
                    break;
                case 4:
                    message.visibility = reader.int32();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            objectName: (0, helpers_1.isSet)(object.objectName) ? String(object.objectName) : "",
            visibility: (0, helpers_1.isSet)(object.visibility) ? (0, common_1.visibilityTypeFromJSON)(object.visibility) : -1
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.objectName !== undefined && (obj.objectName = message.objectName);
        message.visibility !== undefined && (obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility));
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgUpdateObjectInfo();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.objectName = object.objectName ?? "";
        message.visibility = object.visibility ?? 0;
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name,
            objectName: object?.object_name,
            visibility: (0, helpers_1.isSet)(object.visibility) ? (0, common_1.visibilityTypeFromJSON)(object.visibility) : -1
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        message.visibility !== undefined && (obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility));
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgUpdateObjectInfo();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.object_name !== undefined && object.object_name !== null) {
            message.objectName = object.object_name;
        }
        if (object.visibility !== undefined && object.visibility !== null) {
            message.visibility = (0, common_1.visibilityTypeFromJSON)(object.visibility);
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.object_name = message.objectName;
        obj.visibility = (0, common_1.visibilityTypeToJSON)(message.visibility);
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgUpdateObjectInfo.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgUpdateObjectInfo.decode(message.value);
    },
    toProto(message) {
        return exports.MsgUpdateObjectInfo.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgUpdateObjectInfo",
            value: exports.MsgUpdateObjectInfo.encode(message).finish()
        };
    }
};
function createBaseMsgMirrorBucketResponse() {
    return {};
}
exports.MsgMirrorBucketResponse = {
    typeUrl: "/greenfield.storage.MsgMirrorBucketResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgMirrorBucketResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgMirrorBucketResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgMirrorBucketResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgMirrorBucketResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgMirrorBucketResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgMirrorBucketResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgMirrorBucketResponse",
            value: exports.MsgMirrorBucketResponse.encode(message).finish()
        };
    }
};
function createBaseMsgMirrorGroup() {
    return {
        operator: "",
        id: "",
        groupName: "",
        destChainId: 0
    };
}
exports.MsgMirrorGroup = {
    typeUrl: "/greenfield.storage.MsgMirrorGroup",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.id !== "") {
            writer.uint32(18).string(message.id);
        }
        if (message.groupName !== "") {
            writer.uint32(26).string(message.groupName);
        }
        if (message.destChainId !== 0) {
            writer.uint32(32).uint32(message.destChainId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgMirrorGroup();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.id = reader.string();
                    break;
                case 3:
                    message.groupName = reader.string();
                    break;
                case 4:
                    message.destChainId = reader.uint32();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            id: (0, helpers_1.isSet)(object.id) ? String(object.id) : "",
            groupName: (0, helpers_1.isSet)(object.groupName) ? String(object.groupName) : "",
            destChainId: (0, helpers_1.isSet)(object.destChainId) ? Number(object.destChainId) : 0
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.id !== undefined && (obj.id = message.id);
        message.groupName !== undefined && (obj.groupName = message.groupName);
        message.destChainId !== undefined && (obj.destChainId = Math.round(message.destChainId));
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgMirrorGroup();
        message.operator = object.operator ?? "";
        message.id = object.id ?? "";
        message.groupName = object.groupName ?? "";
        message.destChainId = object.destChainId ?? 0;
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            id: object?.id,
            groupName: object?.group_name,
            destChainId: object?.dest_chain_id
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.id = message.id;
        obj.group_name = message.groupName;
        obj.dest_chain_id = message.destChainId;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgMirrorGroup();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.id !== undefined && object.id !== null) {
            message.id = object.id;
        }
        if (object.group_name !== undefined && object.group_name !== null) {
            message.groupName = object.group_name;
        }
        if (object.dest_chain_id !== undefined && object.dest_chain_id !== null) {
            message.destChainId = object.dest_chain_id;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.id = message.id;
        obj.group_name = message.groupName;
        obj.dest_chain_id = message.destChainId;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgMirrorGroup.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgMirrorGroup.decode(message.value);
    },
    toProto(message) {
        return exports.MsgMirrorGroup.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgMirrorGroup",
            value: exports.MsgMirrorGroup.encode(message).finish()
        };
    }
};
function createBaseMsgMirrorGroupResponse() {
    return {};
}
exports.MsgMirrorGroupResponse = {
    typeUrl: "/greenfield.storage.MsgMirrorGroupResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgMirrorGroupResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgMirrorGroupResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgMirrorGroupResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgMirrorGroupResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgMirrorGroupResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgMirrorGroupResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgMirrorGroupResponse",
            value: exports.MsgMirrorGroupResponse.encode(message).finish()
        };
    }
};
function createBaseMsgUpdateParams() {
    return {
        authority: "",
        params: params_1.Params.fromPartial({})
    };
}
exports.MsgUpdateParams = {
    typeUrl: "/greenfield.storage.MsgUpdateParams",
    encode(message, writer = _m0.Writer.create()) {
        if (message.authority !== "") {
            writer.uint32(10).string(message.authority);
        }
        if (message.params !== undefined) {
            params_1.Params.encode(message.params, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgUpdateParams();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.authority = reader.string();
                    break;
                case 2:
                    message.params = params_1.Params.decode(reader, reader.uint32());
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            authority: (0, helpers_1.isSet)(object.authority) ? String(object.authority) : "",
            params: (0, helpers_1.isSet)(object.params) ? params_1.Params.fromJSON(object.params) : undefined
        };
    },
    toJSON(message) {
        const obj = {};
        message.authority !== undefined && (obj.authority = message.authority);
        message.params !== undefined && (obj.params = message.params ? params_1.Params.toJSON(message.params) : undefined);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgUpdateParams();
        message.authority = object.authority ?? "";
        message.params = object.params !== undefined && object.params !== null ? params_1.Params.fromPartial(object.params) : undefined;
        return message;
    },
    fromSDK(object) {
        return {
            authority: object?.authority,
            params: object.params ? params_1.Params.fromSDK(object.params) : undefined
        };
    },
    toSDK(message) {
        const obj = {};
        obj.authority = message.authority;
        message.params !== undefined && (obj.params = message.params ? params_1.Params.toSDK(message.params) : undefined);
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgUpdateParams();
        if (object.authority !== undefined && object.authority !== null) {
            message.authority = object.authority;
        }
        if (object.params !== undefined && object.params !== null) {
            message.params = params_1.Params.fromAmino(object.params);
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.authority = message.authority;
        obj.params = message.params ? params_1.Params.toAmino(message.params) : undefined;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgUpdateParams.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgUpdateParams.decode(message.value);
    },
    toProto(message) {
        return exports.MsgUpdateParams.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgUpdateParams",
            value: exports.MsgUpdateParams.encode(message).finish()
        };
    }
};
function createBaseMsgUpdateParamsResponse() {
    return {};
}
exports.MsgUpdateParamsResponse = {
    typeUrl: "/greenfield.storage.MsgUpdateParamsResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgUpdateParamsResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgUpdateParamsResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgUpdateParamsResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgUpdateParamsResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgUpdateParamsResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgUpdateParamsResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgUpdateParamsResponse",
            value: exports.MsgUpdateParamsResponse.encode(message).finish()
        };
    }
};
function createBaseMsgMigrateBucket() {
    return {
        operator: "",
        bucketName: "",
        dstPrimarySpId: 0,
        dstPrimarySpApproval: undefined
    };
}
exports.MsgMigrateBucket = {
    typeUrl: "/greenfield.storage.MsgMigrateBucket",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        if (message.dstPrimarySpId !== 0) {
            writer.uint32(24).uint32(message.dstPrimarySpId);
        }
        if (message.dstPrimarySpApproval !== undefined) {
            approval_1.Approval.encode(message.dstPrimarySpApproval, writer.uint32(34).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgMigrateBucket();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.dstPrimarySpId = reader.uint32();
                    break;
                case 4:
                    message.dstPrimarySpApproval = approval_1.Approval.decode(reader, reader.uint32());
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            dstPrimarySpId: (0, helpers_1.isSet)(object.dstPrimarySpId) ? Number(object.dstPrimarySpId) : 0,
            dstPrimarySpApproval: (0, helpers_1.isSet)(object.dstPrimarySpApproval) ? approval_1.Approval.fromJSON(object.dstPrimarySpApproval) : undefined
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.dstPrimarySpId !== undefined && (obj.dstPrimarySpId = Math.round(message.dstPrimarySpId));
        message.dstPrimarySpApproval !== undefined && (obj.dstPrimarySpApproval = message.dstPrimarySpApproval ? approval_1.Approval.toJSON(message.dstPrimarySpApproval) : undefined);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgMigrateBucket();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.dstPrimarySpId = object.dstPrimarySpId ?? 0;
        message.dstPrimarySpApproval = object.dstPrimarySpApproval !== undefined && object.dstPrimarySpApproval !== null ? approval_1.Approval.fromPartial(object.dstPrimarySpApproval) : undefined;
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name,
            dstPrimarySpId: object?.dst_primary_sp_id,
            dstPrimarySpApproval: object.dst_primary_sp_approval ? approval_1.Approval.fromSDK(object.dst_primary_sp_approval) : undefined
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.dst_primary_sp_id = message.dstPrimarySpId;
        message.dstPrimarySpApproval !== undefined && (obj.dst_primary_sp_approval = message.dstPrimarySpApproval ? approval_1.Approval.toSDK(message.dstPrimarySpApproval) : undefined);
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgMigrateBucket();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.dst_primary_sp_id !== undefined && object.dst_primary_sp_id !== null) {
            message.dstPrimarySpId = object.dst_primary_sp_id;
        }
        if (object.dst_primary_sp_approval !== undefined && object.dst_primary_sp_approval !== null) {
            message.dstPrimarySpApproval = approval_1.Approval.fromAmino(object.dst_primary_sp_approval);
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.dst_primary_sp_id = message.dstPrimarySpId;
        obj.dst_primary_sp_approval = message.dstPrimarySpApproval ? approval_1.Approval.toAmino(message.dstPrimarySpApproval) : undefined;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgMigrateBucket.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgMigrateBucket.decode(message.value);
    },
    toProto(message) {
        return exports.MsgMigrateBucket.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgMigrateBucket",
            value: exports.MsgMigrateBucket.encode(message).finish()
        };
    }
};
function createBaseMsgMigrateBucketResponse() {
    return {};
}
exports.MsgMigrateBucketResponse = {
    typeUrl: "/greenfield.storage.MsgMigrateBucketResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgMigrateBucketResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgMigrateBucketResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgMigrateBucketResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgMigrateBucketResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgMigrateBucketResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgMigrateBucketResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgMigrateBucketResponse",
            value: exports.MsgMigrateBucketResponse.encode(message).finish()
        };
    }
};
function createBaseMsgCompleteMigrateBucket() {
    return {
        operator: "",
        bucketName: "",
        globalVirtualGroupFamilyId: 0,
        gvgMappings: []
    };
}
exports.MsgCompleteMigrateBucket = {
    typeUrl: "/greenfield.storage.MsgCompleteMigrateBucket",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        if (message.globalVirtualGroupFamilyId !== 0) {
            writer.uint32(24).uint32(message.globalVirtualGroupFamilyId);
        }
        for (const v of message.gvgMappings) {
            common_1.GVGMapping.encode(v, writer.uint32(34).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCompleteMigrateBucket();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                case 3:
                    message.globalVirtualGroupFamilyId = reader.uint32();
                    break;
                case 4:
                    message.gvgMappings.push(common_1.GVGMapping.decode(reader, reader.uint32()));
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : "",
            globalVirtualGroupFamilyId: (0, helpers_1.isSet)(object.globalVirtualGroupFamilyId) ? Number(object.globalVirtualGroupFamilyId) : 0,
            gvgMappings: Array.isArray(object?.gvgMappings) ? object.gvgMappings.map((e) => common_1.GVGMapping.fromJSON(e)) : []
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        message.globalVirtualGroupFamilyId !== undefined && (obj.globalVirtualGroupFamilyId = Math.round(message.globalVirtualGroupFamilyId));
        if (message.gvgMappings) {
            obj.gvgMappings = message.gvgMappings.map(e => e ? common_1.GVGMapping.toJSON(e) : undefined);
        }
        else {
            obj.gvgMappings = [];
        }
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgCompleteMigrateBucket();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        message.globalVirtualGroupFamilyId = object.globalVirtualGroupFamilyId ?? 0;
        message.gvgMappings = object.gvgMappings?.map(e => common_1.GVGMapping.fromPartial(e)) || [];
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name,
            globalVirtualGroupFamilyId: object?.global_virtual_group_family_id,
            gvgMappings: Array.isArray(object?.gvg_mappings) ? object.gvg_mappings.map((e) => common_1.GVGMapping.fromSDK(e)) : []
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.global_virtual_group_family_id = message.globalVirtualGroupFamilyId;
        if (message.gvgMappings) {
            obj.gvg_mappings = message.gvgMappings.map(e => e ? common_1.GVGMapping.toSDK(e) : undefined);
        }
        else {
            obj.gvg_mappings = [];
        }
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgCompleteMigrateBucket();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        if (object.global_virtual_group_family_id !== undefined && object.global_virtual_group_family_id !== null) {
            message.globalVirtualGroupFamilyId = object.global_virtual_group_family_id;
        }
        message.gvgMappings = object.gvg_mappings?.map(e => common_1.GVGMapping.fromAmino(e)) || [];
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        obj.global_virtual_group_family_id = message.globalVirtualGroupFamilyId;
        if (message.gvgMappings) {
            obj.gvg_mappings = message.gvgMappings.map(e => e ? common_1.GVGMapping.toAmino(e) : undefined);
        }
        else {
            obj.gvg_mappings = [];
        }
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCompleteMigrateBucket.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCompleteMigrateBucket.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCompleteMigrateBucket.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCompleteMigrateBucket",
            value: exports.MsgCompleteMigrateBucket.encode(message).finish()
        };
    }
};
function createBaseMsgCompleteMigrateBucketResponse() {
    return {};
}
exports.MsgCompleteMigrateBucketResponse = {
    typeUrl: "/greenfield.storage.MsgCompleteMigrateBucketResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCompleteMigrateBucketResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgCompleteMigrateBucketResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgCompleteMigrateBucketResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCompleteMigrateBucketResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCompleteMigrateBucketResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCompleteMigrateBucketResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCompleteMigrateBucketResponse",
            value: exports.MsgCompleteMigrateBucketResponse.encode(message).finish()
        };
    }
};
function createBaseMsgCancelMigrateBucket() {
    return {
        operator: "",
        bucketName: ""
    };
}
exports.MsgCancelMigrateBucket = {
    typeUrl: "/greenfield.storage.MsgCancelMigrateBucket",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCancelMigrateBucket();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgCancelMigrateBucket();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgCancelMigrateBucket();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCancelMigrateBucket.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCancelMigrateBucket.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCancelMigrateBucket.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCancelMigrateBucket",
            value: exports.MsgCancelMigrateBucket.encode(message).finish()
        };
    }
};
function createBaseMsgCancelMigrateBucketResponse() {
    return {};
}
exports.MsgCancelMigrateBucketResponse = {
    typeUrl: "/greenfield.storage.MsgCancelMigrateBucketResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgCancelMigrateBucketResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgCancelMigrateBucketResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgCancelMigrateBucketResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgCancelMigrateBucketResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgCancelMigrateBucketResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgCancelMigrateBucketResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgCancelMigrateBucketResponse",
            value: exports.MsgCancelMigrateBucketResponse.encode(message).finish()
        };
    }
};
function createBaseMsgRejectMigrateBucket() {
    return {
        operator: "",
        bucketName: ""
    };
}
exports.MsgRejectMigrateBucket = {
    typeUrl: "/greenfield.storage.MsgRejectMigrateBucket",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.bucketName !== "") {
            writer.uint32(18).string(message.bucketName);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgRejectMigrateBucket();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.bucketName = reader.string();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            bucketName: (0, helpers_1.isSet)(object.bucketName) ? String(object.bucketName) : ""
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.bucketName !== undefined && (obj.bucketName = message.bucketName);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgRejectMigrateBucket();
        message.operator = object.operator ?? "";
        message.bucketName = object.bucketName ?? "";
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            bucketName: object?.bucket_name
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgRejectMigrateBucket();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.bucket_name !== undefined && object.bucket_name !== null) {
            message.bucketName = object.bucket_name;
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.bucket_name = message.bucketName;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgRejectMigrateBucket.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgRejectMigrateBucket.decode(message.value);
    },
    toProto(message) {
        return exports.MsgRejectMigrateBucket.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgRejectMigrateBucket",
            value: exports.MsgRejectMigrateBucket.encode(message).finish()
        };
    }
};
function createBaseMsgRejectMigrateBucketResponse() {
    return {};
}
exports.MsgRejectMigrateBucketResponse = {
    typeUrl: "/greenfield.storage.MsgRejectMigrateBucketResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgRejectMigrateBucketResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgRejectMigrateBucketResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgRejectMigrateBucketResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgRejectMigrateBucketResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgRejectMigrateBucketResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgRejectMigrateBucketResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgRejectMigrateBucketResponse",
            value: exports.MsgRejectMigrateBucketResponse.encode(message).finish()
        };
    }
};
function createBaseMsgSetTag() {
    return {
        operator: "",
        resource: "",
        tags: undefined
    };
}
exports.MsgSetTag = {
    typeUrl: "/greenfield.storage.MsgSetTag",
    encode(message, writer = _m0.Writer.create()) {
        if (message.operator !== "") {
            writer.uint32(10).string(message.operator);
        }
        if (message.resource !== "") {
            writer.uint32(18).string(message.resource);
        }
        if (message.tags !== undefined) {
            types_1.ResourceTags.encode(message.tags, writer.uint32(26).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgSetTag();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.operator = reader.string();
                    break;
                case 2:
                    message.resource = reader.string();
                    break;
                case 3:
                    message.tags = types_1.ResourceTags.decode(reader, reader.uint32());
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        return {
            operator: (0, helpers_1.isSet)(object.operator) ? String(object.operator) : "",
            resource: (0, helpers_1.isSet)(object.resource) ? String(object.resource) : "",
            tags: (0, helpers_1.isSet)(object.tags) ? types_1.ResourceTags.fromJSON(object.tags) : undefined
        };
    },
    toJSON(message) {
        const obj = {};
        message.operator !== undefined && (obj.operator = message.operator);
        message.resource !== undefined && (obj.resource = message.resource);
        message.tags !== undefined && (obj.tags = message.tags ? types_1.ResourceTags.toJSON(message.tags) : undefined);
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgSetTag();
        message.operator = object.operator ?? "";
        message.resource = object.resource ?? "";
        message.tags = object.tags !== undefined && object.tags !== null ? types_1.ResourceTags.fromPartial(object.tags) : undefined;
        return message;
    },
    fromSDK(object) {
        return {
            operator: object?.operator,
            resource: object?.resource,
            tags: object.tags ? types_1.ResourceTags.fromSDK(object.tags) : undefined
        };
    },
    toSDK(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.resource = message.resource;
        message.tags !== undefined && (obj.tags = message.tags ? types_1.ResourceTags.toSDK(message.tags) : undefined);
        return obj;
    },
    fromAmino(object) {
        const message = createBaseMsgSetTag();
        if (object.operator !== undefined && object.operator !== null) {
            message.operator = object.operator;
        }
        if (object.resource !== undefined && object.resource !== null) {
            message.resource = object.resource;
        }
        if (object.tags !== undefined && object.tags !== null) {
            message.tags = types_1.ResourceTags.fromAmino(object.tags);
        }
        return message;
    },
    toAmino(message) {
        const obj = {};
        obj.operator = message.operator;
        obj.resource = message.resource;
        obj.tags = message.tags ? types_1.ResourceTags.toAmino(message.tags) : undefined;
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgSetTag.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgSetTag.decode(message.value);
    },
    toProto(message) {
        return exports.MsgSetTag.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgSetTag",
            value: exports.MsgSetTag.encode(message).finish()
        };
    }
};
function createBaseMsgSetTagResponse() {
    return {};
}
exports.MsgSetTagResponse = {
    typeUrl: "/greenfield.storage.MsgSetTagResponse",
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : new _m0.Reader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgSetTagResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    fromPartial(_) {
        const message = createBaseMsgSetTagResponse();
        return message;
    },
    fromSDK(_) {
        return {};
    },
    toSDK(_) {
        const obj = {};
        return obj;
    },
    fromAmino(_) {
        const message = createBaseMsgSetTagResponse();
        return message;
    },
    toAmino(_) {
        const obj = {};
        return obj;
    },
    fromAminoMsg(object) {
        return exports.MsgSetTagResponse.fromAmino(object.value);
    },
    fromProtoMsg(message) {
        return exports.MsgSetTagResponse.decode(message.value);
    },
    toProto(message) {
        return exports.MsgSetTagResponse.encode(message).finish();
    },
    toProtoMsg(message) {
        return {
            typeUrl: "/greenfield.storage.MsgSetTagResponse",
            value: exports.MsgSetTagResponse.encode(message).finish()
        };
    }
};
class MsgClientImpl {
    constructor(rpc) {
        this.rpc = rpc;
        this.CreateBucket = this.CreateBucket.bind(this);
        this.DeleteBucket = this.DeleteBucket.bind(this);
        this.UpdateBucketInfo = this.UpdateBucketInfo.bind(this);
        this.MirrorBucket = this.MirrorBucket.bind(this);
        this.DiscontinueBucket = this.DiscontinueBucket.bind(this);
        this.CreateObject = this.CreateObject.bind(this);
        this.SealObject = this.SealObject.bind(this);
        this.RejectSealObject = this.RejectSealObject.bind(this);
        this.CopyObject = this.CopyObject.bind(this);
        this.DeleteObject = this.DeleteObject.bind(this);
        this.CancelCreateObject = this.CancelCreateObject.bind(this);
        this.MirrorObject = this.MirrorObject.bind(this);
        this.DiscontinueObject = this.DiscontinueObject.bind(this);
        this.UpdateObjectInfo = this.UpdateObjectInfo.bind(this);
        this.CreateGroup = this.CreateGroup.bind(this);
        this.DeleteGroup = this.DeleteGroup.bind(this);
        this.UpdateGroupMember = this.UpdateGroupMember.bind(this);
        this.UpdateGroupExtra = this.UpdateGroupExtra.bind(this);
        this.LeaveGroup = this.LeaveGroup.bind(this);
        this.MirrorGroup = this.MirrorGroup.bind(this);
        this.RenewGroupMember = this.RenewGroupMember.bind(this);
        this.PutPolicy = this.PutPolicy.bind(this);
        this.DeletePolicy = this.DeletePolicy.bind(this);
        this.UpdateParams = this.UpdateParams.bind(this);
        this.MigrateBucket = this.MigrateBucket.bind(this);
        this.CompleteMigrateBucket = this.CompleteMigrateBucket.bind(this);
        this.CancelMigrateBucket = this.CancelMigrateBucket.bind(this);
        this.RejectMigrateBucket = this.RejectMigrateBucket.bind(this);
        this.SetTag = this.SetTag.bind(this);
    }
    CreateBucket(request) {
        const data = exports.MsgCreateBucket.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "CreateBucket", data);
        return promise.then(data => exports.MsgCreateBucketResponse.decode(new _m0.Reader(data)));
    }
    DeleteBucket(request) {
        const data = exports.MsgDeleteBucket.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "DeleteBucket", data);
        return promise.then(data => exports.MsgDeleteBucketResponse.decode(new _m0.Reader(data)));
    }
    UpdateBucketInfo(request) {
        const data = exports.MsgUpdateBucketInfo.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "UpdateBucketInfo", data);
        return promise.then(data => exports.MsgUpdateBucketInfoResponse.decode(new _m0.Reader(data)));
    }
    MirrorBucket(request) {
        const data = exports.MsgMirrorBucket.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "MirrorBucket", data);
        return promise.then(data => exports.MsgMirrorBucketResponse.decode(new _m0.Reader(data)));
    }
    DiscontinueBucket(request) {
        const data = exports.MsgDiscontinueBucket.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "DiscontinueBucket", data);
        return promise.then(data => exports.MsgDiscontinueBucketResponse.decode(new _m0.Reader(data)));
    }
    CreateObject(request) {
        const data = exports.MsgCreateObject.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "CreateObject", data);
        return promise.then(data => exports.MsgCreateObjectResponse.decode(new _m0.Reader(data)));
    }
    SealObject(request) {
        const data = exports.MsgSealObject.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "SealObject", data);
        return promise.then(data => exports.MsgSealObjectResponse.decode(new _m0.Reader(data)));
    }
    RejectSealObject(request) {
        const data = exports.MsgRejectSealObject.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "RejectSealObject", data);
        return promise.then(data => exports.MsgRejectSealObjectResponse.decode(new _m0.Reader(data)));
    }
    CopyObject(request) {
        const data = exports.MsgCopyObject.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "CopyObject", data);
        return promise.then(data => exports.MsgCopyObjectResponse.decode(new _m0.Reader(data)));
    }
    DeleteObject(request) {
        const data = exports.MsgDeleteObject.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "DeleteObject", data);
        return promise.then(data => exports.MsgDeleteObjectResponse.decode(new _m0.Reader(data)));
    }
    CancelCreateObject(request) {
        const data = exports.MsgCancelCreateObject.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "CancelCreateObject", data);
        return promise.then(data => exports.MsgCancelCreateObjectResponse.decode(new _m0.Reader(data)));
    }
    MirrorObject(request) {
        const data = exports.MsgMirrorObject.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "MirrorObject", data);
        return promise.then(data => exports.MsgMirrorObjectResponse.decode(new _m0.Reader(data)));
    }
    DiscontinueObject(request) {
        const data = exports.MsgDiscontinueObject.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "DiscontinueObject", data);
        return promise.then(data => exports.MsgDiscontinueObjectResponse.decode(new _m0.Reader(data)));
    }
    UpdateObjectInfo(request) {
        const data = exports.MsgUpdateObjectInfo.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "UpdateObjectInfo", data);
        return promise.then(data => exports.MsgUpdateObjectInfoResponse.decode(new _m0.Reader(data)));
    }
    CreateGroup(request) {
        const data = exports.MsgCreateGroup.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "CreateGroup", data);
        return promise.then(data => exports.MsgCreateGroupResponse.decode(new _m0.Reader(data)));
    }
    DeleteGroup(request) {
        const data = exports.MsgDeleteGroup.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "DeleteGroup", data);
        return promise.then(data => exports.MsgDeleteGroupResponse.decode(new _m0.Reader(data)));
    }
    UpdateGroupMember(request) {
        const data = exports.MsgUpdateGroupMember.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "UpdateGroupMember", data);
        return promise.then(data => exports.MsgUpdateGroupMemberResponse.decode(new _m0.Reader(data)));
    }
    UpdateGroupExtra(request) {
        const data = exports.MsgUpdateGroupExtra.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "UpdateGroupExtra", data);
        return promise.then(data => exports.MsgUpdateGroupExtraResponse.decode(new _m0.Reader(data)));
    }
    LeaveGroup(request) {
        const data = exports.MsgLeaveGroup.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "LeaveGroup", data);
        return promise.then(data => exports.MsgLeaveGroupResponse.decode(new _m0.Reader(data)));
    }
    MirrorGroup(request) {
        const data = exports.MsgMirrorGroup.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "MirrorGroup", data);
        return promise.then(data => exports.MsgMirrorGroupResponse.decode(new _m0.Reader(data)));
    }
    RenewGroupMember(request) {
        const data = exports.MsgRenewGroupMember.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "RenewGroupMember", data);
        return promise.then(data => exports.MsgRenewGroupMemberResponse.decode(new _m0.Reader(data)));
    }
    PutPolicy(request) {
        const data = exports.MsgPutPolicy.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "PutPolicy", data);
        return promise.then(data => exports.MsgPutPolicyResponse.decode(new _m0.Reader(data)));
    }
    DeletePolicy(request) {
        const data = exports.MsgDeletePolicy.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "DeletePolicy", data);
        return promise.then(data => exports.MsgDeletePolicyResponse.decode(new _m0.Reader(data)));
    }
    UpdateParams(request) {
        const data = exports.MsgUpdateParams.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "UpdateParams", data);
        return promise.then(data => exports.MsgUpdateParamsResponse.decode(new _m0.Reader(data)));
    }
    MigrateBucket(request) {
        const data = exports.MsgMigrateBucket.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "MigrateBucket", data);
        return promise.then(data => exports.MsgMigrateBucketResponse.decode(new _m0.Reader(data)));
    }
    CompleteMigrateBucket(request) {
        const data = exports.MsgCompleteMigrateBucket.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "CompleteMigrateBucket", data);
        return promise.then(data => exports.MsgCompleteMigrateBucketResponse.decode(new _m0.Reader(data)));
    }
    CancelMigrateBucket(request) {
        const data = exports.MsgCancelMigrateBucket.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "CancelMigrateBucket", data);
        return promise.then(data => exports.MsgCancelMigrateBucketResponse.decode(new _m0.Reader(data)));
    }
    RejectMigrateBucket(request) {
        const data = exports.MsgRejectMigrateBucket.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "RejectMigrateBucket", data);
        return promise.then(data => exports.MsgRejectMigrateBucketResponse.decode(new _m0.Reader(data)));
    }
    SetTag(request) {
        const data = exports.MsgSetTag.encode(request).finish();
        const promise = this.rpc.request("greenfield.storage.Msg", "SetTag", data);
        return promise.then(data => exports.MsgSetTagResponse.decode(new _m0.Reader(data)));
    }
}
exports.MsgClientImpl = MsgClientImpl;
//# sourceMappingURL=tx.js.map